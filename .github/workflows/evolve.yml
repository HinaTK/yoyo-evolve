name: Legacy Yoyo Evolution Disabled

on:
  workflow_dispatch:

concurrency:
  group: legacy-yoyo-evolution-disabled
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Legacy yoyo self-evolution is disabled for this repository."
          echo "Use the Investment System Evolution workflow instead."
