# 2026-01-22 每日研究计划（historical replay）

## ?????????

**市场状态：** `risk_on`，但本轮为历史回放，且输入快照存在 `as_of_date=2026-01-22` 与报价字段显示 `2026-04-29` 的非日期对齐问题；同时 `actionable_candidates=[]`。因此今天只能做候选排序、veto audit 与同主题相对强弱复核，不能升级为 `accumulate` / `buy_candidate`。

**最强主题：**
1. `utilities-defensive`：主题均分 75.47，`0002.HK` 领先 `0006.HK`，两者都在上行结构内，但均有 `symbol_risk_veto`，只能观察。
2. `telecom-dividend`：主题均分 75.00，`0941.HK` 分数 81.98，高于 `0728.HK` 的 68.01，且量能扩张更明确；但 `0941.HK` 被历史胜率与选择错误 veto。
3. `energy`：主题均分 69.67，`0857.HK` 分数 84.42 为最高；`0883.HK` 也强但非主题第一且有严重历史负收益记录，`0386.HK` 量能最强但趋势不足且不是主题 leader。
4. `financials-exchange`：`0388.HK` 分数 67.30，单一主题代表，1 日上涨 2.99% 且 `volume-expansion`，但仍被 `symbol_risk_veto` 限制。
5. `internet-platform`：主题均分仅 32.34，但内部 `9618.HK` 分数 72.27 明显优于 `9988.HK`、`3690.HK`、`0700.HK`、`1024.HK`；由于该主题近期 selected-vs-best 错误密集，只能做相对强弱审计。

**最弱主题：**
- `healthcare-pharma`、`healthcare-biotech`、`consumer-tech`：主题均分为 0 或接近 0，多数成分处于 `downtrend`、低区间位置或价格低于均线，今天不应挖掘反弹交易。
- `hang-seng-tech`：ETF `3067.HK` / `3033.HK` 未达 watch 分数，且科技 ETF 历史 pass_rate 偏低；只能作为风险偏好确认，不作为升级依据。
- `consumer-discretionary`：`2020.HK` 是主题 leader，但量能不足且有 `symbol_risk_veto`；`2331.HK`、`9992.HK`、`6862.HK` 均不具备行动资格。

**当前交易宇宙未覆盖的机会：**
- 若要确认 `energy` 与 `telecom-dividend` 的主题强度，当前宇宙缺少更直接的港股高股息 ETF、能源 ETF 或大宗商品价格代理；今天只能用同主题股票内部扩散度作弱确认，不能把主题判断升级为交易信号。
- 对广义港股风险偏好，`2800.HK` 仍是可用代理，但其近期 T+3 / T+5 misfire 与 `symbol_risk_veto` 使其只能作为确认指标，不适合作为 fallback 买入候选。

## 今日结论

- **推荐模式：** recommendation_only，不假设真实持仓。
- **行动上限：** `watch_only` / `avoid`。原因是 `actionable_candidates=[]`，且核心诊断候选均有 `symbol_risk_veto` 或非日期对齐问题。
- **今日任务：** 不寻找替代买点；重点审计 top diagnostic candidates 的 veto 是否仍有效，并比较同主题 peer 是否出现真实相对强弱改善。
- **成本门槛：** 估算往返成本 35 bps，最低 edge 100 bps，且需要 edge 至少为成本 2 倍；当前没有任何候选满足升级证据。

## ????????

| 排名 | symbol | theme | 状态 | 关注理由 | 升级可能性 |
|---:|---|---|---|---|---|
| 1 | `0857.HK` | `energy` | `watch_only` / veto audit | 分数 84.42，主题第一，价格高于 MA20/MA60，MA20>MA60，量能确认，动量强 | 无；`symbol_risk_veto` 且无 `actionable_candidates` |
| 2 | `0941.HK` | `telecom-dividend` | `watch_only` / veto audit | 分数 81.98，趋势 100，量能扩张，主题第一 | 无；历史 pass_rate=0.000 over 30 evaluated calls |
| 3 | `0002.HK` | `utilities-defensive` | `watch_only` / defensive audit | 分数 76.08，防御主题 leader，趋势结构稳 | 无；样本少但 pass_rate=0.000，仍被 veto |
| 4 | `9618.HK` | `internet-platform` | `watch_only` / peer-relative review | 互联网平台中唯一高分 leader，强于 `0700.HK` 与 `9988.HK` | 无；`symbol_risk_veto`，且互联网主题近期选择错误多 |
| 5 | `0388.HK` | `financials-exchange` | `watch_only` / volume audit | 分数 67.30，1 日 +2.99%，量能 1.5564，交易所 beta 对 risk_on 敏感 | 无；`symbol_risk_veto`，且仅为诊断候选 |

## 候选研究计划

### 1. `0857.HK` — PetroChina / 中国石油股份

**今日状态：** `watch_only`，仅做 veto audit。

**为什么今天值得关注：**
- `energy` 是强主题之一，主题均分 69.67。
- `0857.HK` 是主题 leader，score=84.42，trend_score=100，momentum_score=75.99。
- 技术结构完整：latest_close=11.98，高于 ma20=10.853 与 ma60=10.2045，且 ma20>ma60。
- volume_ratio_20=1.2356，说明上涨并非完全缺量。

**同主题比较：**
- 优于 `0883.HK`：`0857.HK` score 84.42 高于 `0883.HK` 73.93，且是 theme leader；`0883.HK` 虽处于上行，但被严重历史负收益与 adverse breach 拖累。
- 优于 `0386.HK`：`0386.HK` volume_ratio_20=2.2023 更强，但 score 50.67、ma60 仍高于现价，趋势质量不如 `0857.HK`；同时它是 same-theme non-leader，不能替代 veto leader。

**升级前缺失证据：**
- 必须出现在 `actionable_candidates`，或明确 `qualified_for_action=true`。
- 需要日期对齐报价，不能用 `2026-04-29` 报价字段直接支持 `2026-01-22` 行动。
- 需要相对 `0883.HK` 与 `0386.HK` 的持续 3–5 日强势，而不是单日排名领先。
- 需要解除或解释 `symbol_risk_veto`：pass_rate=0.091 over 33 evaluated calls、avg_return_pct 为负、存在 selection_error。
- 需要预设失效：若跌回 ma20 或 energy 主题扩散转弱，则继续 `watch_only`。

**风险：**
- 价格 range_pos_60=1.1084，可能已短线过热。
- 历史评估对该 symbol 不友好，追高易触发回撤。

### 2. `0941.HK` — China Mobile / 中国移动

**今日状态：** `watch_only`，仅做 veto audit。

**为什么今天值得关注：**
- `telecom-dividend` 主题均分 75.00，属于强主题。
- `0941.HK` score=81.98，trend_score=100，price_above_ma20、price_above_ma60、ma20_above_ma60 均成立。
- volume_ratio_20=1.5041，`regime_flags` 包含 `uptrend` 与 `volume-expansion`。

**同主题比较：**
- 优于 `0728.HK`：`0941.HK` 分数 81.98 高于 `0728.HK` 68.01，且 MA20>MA60；`0728.HK` 虽动量和量能也强，但不是主题 leader，且 ma20 仍低于 ma60，趋势确认弱一层。
- `0728.HK` 不能作为替代行动候选，因为它有 `not_theme_score_leader` 与 `symbol_risk_veto`，且没有进入 `actionable_candidates`。

**升级前缺失证据：**
- 必须进入 `actionable_candidates`。
- 需要确认高股息/电信主题不是纯防御拥挤交易，最好有 ETF 或指数级别确认。
- 需要 `0941.HK` 相对 `0728.HK` 的强势继续扩大，而不是同涨后的轮动追高。
- 需要解除 `symbol_risk_veto`：pass_rate=0.000 over 30 evaluated calls、avg_return_pct 为负、存在 recent adverse breach。
- 失效条件：跌破 ma20=81.55 或量能扩张消失且主题 leader 地位让位给 `0728.HK`。

**风险：**
- range_pos_60=1.1119，短线位置偏高。
- 历史样本显示该 symbol 的过往调用表现极弱，不能因防御属性而放松门槛。

### 3. `0002.HK` — CLP Holdings / 中电控股

**今日状态：** `watch_only`，防御主题审计。

**为什么今天值得关注：**
- `utilities-defensive` 是本轮最高均分主题，avg_score=75.47。
- `0002.HK` 是主题 leader，score=76.08，高于 `0006.HK` 的 74.86。
- 价格高于 ma20=74.5825 与 ma60=73.9937，ma20>ma60，volume_ratio_20=1.2769。

**同主题比较：**
- 优于 `0006.HK`：`0002.HK` 是 theme leader，且当日上涨 0.46%；`0006.HK` 当日 -0.38%，虽分数接近但不是 leader，并有 `not_theme_score_leader`。
- 两者都处于高 range_pos_60，说明防御股可能已被资金提前配置，不能因为稳健而自动升级。

**升级前缺失证据：**
- 必须进入 `actionable_candidates`。
- 需要确认防御主题强不是市场风险偏好转弱前的拥挤避险信号。
- 需要比 `0006.HK` 更持续的相对强度与更清晰的成交确认。
- 需要处理 `symbol_risk_veto`：pass_rate=0.000 over 3 evaluated calls、avg_return_pct 为负；虽然样本少，但不足以支持行动。
- 失效条件：跌破 ma60 或 `0006.HK` 重新成为同主题强者。

**风险：**
- defensive 主题在 `risk_on` 环境里可能只是滞后补涨，不一定有足够 swing edge。
- 样本量少但表现不佳，不能把低波动误判为低风险。

### 4. `9618.HK` — JD.com / 京东集团-SW

**今日状态：** `watch_only`，互联网平台相对强弱复核。

**为什么今天值得关注：**
- `internet-platform` 主题整体弱，但 `9618.HK` 是内部明显 leader：score=72.27，远高于 `9988.HK` 44.75、`3690.HK` 44.67、`0700.HK` 0、`1024.HK` 0。
- 技术结构优于多数同业：latest_close=117.5，高于 ma20=115.5027 与 ma60=107.9074，ma20>ma60。
- range_pos_60=0.7952，说明它不是低位弱反弹，而是相对趋势更强。

**同主题比较：**
- 优于 `9988.HK`：`9988.HK` 单日涨幅 3.24%，但仍低于 ma60，score 低于 watch 阈值且有密集 selection_error。
- 优于 `3690.HK`：`3690.HK` 是单日 leader（+3.55%），但价格仍低于 ma20 与 ma60，并有 price_below_ma20_and_ma60 disqualifier。
- 优于 `0700.HK` 与 `1024.HK`：两者均为 `downtrend`，range_pos_60 极低，不适合做强势候选。

**升级前缺失证据：**
- 必须进入 `actionable_candidates`；当前只是 diagnostic_only。
- 需要相对 `9988.HK`、`3690.HK`、`0700.HK` 的连续强势，而非单日排名。
- 需要解决该主题近期 repeated selected-vs-best misses；未来升级必须说明最近 best peer 与 `9618.HK` 的新相对强度。
- 需要成交量改善：volume_ratio_20=0.7545，尚未充分确认。
- 失效条件：跌回 ma20 或互联网平台主题继续弱于大盘 ETF `2800.HK`。

**风险：**
- 主题均分仅 32.34，个股强不代表主题可交易。
- `symbol_risk_veto` 显示 pass_rate=0.000 over 2 evaluated calls，样本虽少但不能忽略。

### 5. `0388.HK` — HKEX / 香港交易所

**今日状态：** `watch_only`，量能突破审计。

**为什么今天值得关注：**
- `0388.HK` score=67.30，达到 action score 但仍不是 actionable，因为被 veto 且不在 `actionable_candidates`。
- 单日涨幅 2.99%，volume_ratio_20=1.5564，`regime_flags` 包含 `volume-expansion`。
- 价格高于 ma20=406.12 与 ma60=406.7173，且 range_pos_60=0.6766，处于建设性区间。

**同主题比较：**
- `financials-exchange` 在当前宇宙中只有 `0388.HK`，无法做同主题股票替代比较；因此必须用 `2800.HK`、`0005.HK`、`1299.HK` 作为弱金融/市场风险偏好参照。
- 相比 `0005.HK`，`0388.HK` 动量与量能更强；相比 `1299.HK`，`0388.HK` 分数更高且更接近行动阈值。但它不是银行或保险主题，不能直接互相替代。

**升级前缺失证据：**
- 必须进入 `actionable_candidates`。
- 需要日期对齐成交与价格确认，避免用未来报价判断历史日。
- 需要成交扩张延续至少 2–3 个交易日，并确认大盘成交活跃度同步改善。
- 需要解除 `symbol_risk_veto`：pass_rate=0.000 over 4 evaluated calls。
- 失效条件：跌回 ma20/ma60 下方，或量能扩张无法延续。

**风险：**
- 对市场成交、IPO 与风险偏好敏感，若 `risk_on` 不可持续，回撤可能快。
- 样本少但全无 pass，不适合直接升级。

## Disqualifiers

以下条件今天强制将候选限制为 `watch_only` 或 `avoid`：

1. `actionable_candidates=[]`：没有任何标的可被考虑升级为 `accumulate` / `buy_candidate`。
2. 多数高分候选均为 `diagnostic_only=true`，且包含 `symbol_risk_veto`。
3. 历史回放输入存在非日期对齐报价：`as_of_date=2026-01-22`，但行情字段显示 `quote_trade_date=2026-04-29`；只能用于审计与条件计划。
4. 稳定规则要求：`risk_on` 不能覆盖空行动列表、非日期对齐报价或 `symbol_risk_veto`。
5. same-theme substitute 不可替代 veto leader：例如 `0386.HK` 虽无 symbol risk veto、量能强，但不是 `energy` leader，也没有进入 action list。
6. `2800.HK` 不能作为 broad-market fallback：近期 T+3 / T+5 misfire 与低 pass_rate 仍未修复。
7. `hang-seng-tech` ETF 未达 watch 阈值，且历史 pass_rate 偏低，不能作为科技股升级确认。
8. 低位 downtrend 反弹不构成行动信号：`0700.HK`、`1024.HK`、`1810.HK`、`2269.HK`、`1177.HK`、`1093.HK` 当前均不应升级。

## 今日执行清单

- [ ] 复核 `0857.HK`、`0941.HK`、`0002.HK` 的 veto 原因是否仍由 posterior evidence 支持。
- [ ] 对 `energy` 做三者比较：`0857.HK` vs `0883.HK` vs `0386.HK`，重点看是否出现可持续扩散，而非单一 leader 过热。
- [ ] 对 `telecom-dividend` 做二者比较：`0941.HK` vs `0728.HK`，观察量能扩张是否延续。
- [ ] 对 `internet-platform` 只做 peer-relative review：`9618.HK` 是否继续领先 `9988.HK`、`3690.HK`、`0700.HK`、`1024.HK`。
- [ ] 不更新长期记忆；本次输入已显示重复模式，但今天任务是基线计划，除非后续窗口继续验证，不新增 stable learning。

## 风险控制与失效条件

- 单一标的最大建议仓位上限仍为 10%，但今天无实际建仓建议。
- 主题暴露上限 30%，但今天不做组合配置。
- 若任何候选未能提供至少 3 条当前、日期对齐、可验证证据，则保持 `watch_only`。
- 若候选没有明确失效位、相对强弱确认与成本后 edge，则不得升级。
- 若后续 ranking 仍显示 `actionable_candidates=[]`，继续执行 veto audit，不从低排名标的中挖替代行动。
