# 2026-01-08 投资复盘反思（historical）

## 结论摘要

今天的动态排名给出一个表面上的行动候选：`0005.HK`，但我不会把它解释为可直接执行的高置信建议。关键原因是本次历史回放的 `as_of_date=2026-01-08`，而快照内报价字段大量显示 `quote_trade_date=2026-04-29`、`as_of=2026-04-29`。这使得 `actionable_candidates` 只能作为条件审计清单，而不是日期对齐的交易信号。

因此今天的基线建议应为：

- `0005.HK`：低置信条件型 `watch_only` / 审计候选。只有在日期对齐数据中仍保持 price above MA20/MA60、MA20 above MA60，且相对金融板块没有明显落后时，才可重新评估为 `buy_candidate`。
- `0857.HK`、`0941.HK`、`0002.HK`：只作诊断观察，不升级。三者分数更高或接近，但均被 `symbol_risk_veto` 拦截，后验记录显示低通过率、负平均收益或近期不利突破。
- 互联网平台、恒生科技、宽基 ETF：继续保持 `watch_only`。后验对 `0700.HK`、`9988.HK`、`2800.HK`、`3033.HK`、`3067.HK` 的低 pass_rate 和近期 misfire 仍然要求更强的 ETF、广度、成交与均线确认。

## 事实与解释分离

### 可观察事实

- 市场摘要显示 `risk_state=risk_on`，平均股票单日涨幅约 `1.606%`，ETF 单日涨幅约 `1.7%`。
- 当日领涨包括 `3690.HK`、`9988.HK`、`0388.HK`，但这几只多数仍有均线或风险记录约束。
- `0005.HK` 是唯一 `actionable_candidates`：score `71.01`，`qualified_for_action=true`，无 `symbol_risk_veto`。
- 分数最高的 `0857.HK` score `84.42`、`0941.HK` score `81.98`、`0002.HK` score `76.08` 均为 `diagnostic_only=true`，且有 `symbol_risk_veto`。
- `0005.HK` 的 `volume_ratio_20=0.7907`，不是强成交确认；其优势主要来自上升趋势与均线结构。
- 快照日期严重不一致：回放日期为 `2026-01-08`，报价时间却是 `2026-04-29`。

### 我的解释

今天更像是一次“排名系统审计”，不是一次可以直接落地的交易日复盘。`0005.HK` 的结构相对干净，但由于报价日期不匹配、成交确认不足、且没有额外的日期对齐市场广度证据，我只能把它列为下一轮优先验证对象。

对高分但被 veto 的标的，不能因为趋势分高就覆盖后验风险。`0857.HK` 过去 31 条评估记录平均回报约 `-11.390%` 且出现 `symbol_selection_error`；`0941.HK` 28 次评估 pass_rate 仅 `0.071`；`0002.HK` 3 次评估 pass_rate 为 `0.000`。这些不是单次噪音，足以压制行动升级。

## 信心最弱的地方

1. **日期对齐信心最弱**：`as_of_date` 与报价字段冲突是今天最大的证据缺口，任何从价格、均线、成交推导出的结论都必须降级。
2. **短线时点信心弱**：后验摘要显示近期 T+3/T+5 misfire 仍多，尤其 `2800.HK`、`9988.HK`、科技 ETF 的短线升级容易过早。
3. **动态选股信心弱**：`symbol_selection_error` 累计 `163`，近期互联网平台内多次错过更强 peer，说明单靠模型 leader 或单日涨幅仍不可靠。
4. **策略优化信心弱**：参数优化没有更新 active strategy；champion 样本只有 `16`，win_rate `0.25`，avg_net_return_pct `-0.222%`，不足以支持放宽门槛。

## 仍缺失的证据

- 与 `2026-01-08` 完全日期对齐的收盘价、成交量、MA20/MA60、range_pos_60。
- 当日宽基与行业 ETF 的真实确认，尤其 `2800.HK`、`3033.HK`、`3067.HK` 是否同步站上关键均线并放量。
- `0005.HK` 相对金融同业与恒指的当日/近 3 日相对强弱，而不只是绝对均线结构。
- 对 `0857.HK`、`0941.HK`、`0002.HK` 的 veto 是否来自稳定错误模式，还是历史选择条件与当前条件不一致；在未澄清前不能升级。
- 真实 T+3/T+5 风险窗口下的最大不利波动与止损触发概率。

## 今日建议与失效条件

### `0005.HK` — 条件审计候选，实际状态 `watch_only`

- **理由**：唯一进入 `actionable_candidates`，趋势结构较好：收盘 `141.0` 高于 MA20 `137.425` 与 MA60 `133.1856`，MA20 高于 MA60，且无 `symbol_risk_veto`。
- **限制**：成交比 `0.7907` 不强；更重要的是报价日期不匹配。
- **触发条件**：在日期对齐数据中仍满足 price above MA20/MA60、MA20 above MA60，且金融/银行主题没有被更强 peer 明显替代。
- **失效条件**：跌回 MA20 下方、相对 `2800.HK` 或金融主题明显走弱、或日期对齐后发现今日结构并不存在。
- **时间范围**：T+3 到 T+10 的 swing 观察。
- **信心**：低。结构可审计，但证据不够直接行动。

### `0857.HK`、`0941.HK`、`0002.HK` — 诊断观察，不升级

- **理由**：分数和趋势强，但均被 `symbol_risk_veto` 拦截。
- **失效/转强条件**：只有当后续日期对齐数据证明其相对同主题 peer 连续改善、成交维持确认、且风险 veto 被新的样本修复时，才可重新评估。
- **信心**：中等偏高地维持“不升级”。

### 科技/互联网与宽基 ETF — `watch_only`

- **理由**：`3033.HK`、`3067.HK` 分数低于 watch 阈值；`0700.HK`、`1024.HK`、`1810.HK` 处于 downtrend 或 range_pos_60 极低；`9988.HK` 单日强但总分仍低于 watch 阈值且后验低 pass_rate。
- **失效/转强条件**：需要 `2800.HK` 与科技 ETF 同步改善，MA60 收复、成交确认、且互联网平台内部不再重复 selected-vs-best 落后。
- **信心**：中等。继续观察比追涨更符合后验纪律。

## 1-3 个可能失败模式

1. **日期错配导致的虚假行动信号**：`0005.HK` 看似合格，但如果价格/均线实际来自 `2026-04-29`，则对 `2026-01-08` 没有交易意义。这属于风险控制错误。
2. **动态选股的 symbol-selection error**：高分 leader 或表面干净候选可能不是同主题未来最佳表达；近期互联网平台多次 selected-vs-best 落后说明这种错误仍高发。
3. **timing error**：即使方向判断正确，T+3/T+5 仍可能先回撤，尤其在单日 risk_on 后缺少成交和 ETF 跟随时。

## 动态选择错误分类

今天的候选来自动态 trade universe ranking，因此需要预先标注未来错误类型：

- `0005.HK` 若失败：优先归类为 **risk-control error**（因为日期错配仍升级）或 **timing error**（趋势成立但短线追高过早）。
- `0857.HK`、`0941.HK`、`0002.HK` 若被强行升级后失败：归类为 **risk-control error**，因为忽视了 `symbol_risk_veto`。
- 互联网平台若重新选择 `0700.HK`/`9988.HK` 但落后 `9618.HK`、`3690.HK` 或其他 peer：归类为 **symbol-selection error**，不是简单 theme error。
- 若整个 risk_on 主题后续失败：才归类为 **theme error**。

## 下一周期优先级调整

1. **先修复数据对齐**：任何回放信号必须先确认 `as_of_date`、`quote_trade_date`、MA 数据日期一致；否则最高只能 `watch_only`。
2. **把 `0005.HK` 作为验证对象而非买入对象**：下一轮只检查它是否在日期对齐数据中继续保持金融主题强势。
3. **对被 veto 的高分标的建立“解除 veto 条件”**：不因一次高分解除，需要连续 peer-relative 改善、成交确认、且后验风险样本不再恶化。
4. **互联网平台继续做 peer-relative 审计**：不要机械回到 `0700.HK` 或 `9988.HK`；若主题改善，先比较 `9618.HK`、`3690.HK`、`9988.HK`、`0700.HK` 的相对强弱和 ETF 确认。
5. **不放宽策略参数**：优化摘要没有通过样本、改善、胜率与质量门槛，下一周期继续使用保守门槛。
