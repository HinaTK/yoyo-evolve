# 2026-01-09 每日研究计划（historical）

> 模式：recommendation_only。未提供真实持仓，本计划只用于候选排序与条件触发研究，不视为实盘组合管理。  
> 重要数据质量提示：本轮 replay 的 `as_of_date` 为 `2026-01-09`，但行情字段 `quote_trade_date` 为 `2026-04-29`。按稳定规则，所有结论降级为低置信条件审计项；即使 `actionable_candidates` 非空，也不能直接升级为实盘买入，只能列为待验证计划。

## ?????????（市场机会雷达）

### 最强主题
1. **utilities-defensive**：主题均分 `75.47`，`0002.HK` 分数 `76.08`，`0006.HK` 分数 `74.86`。两者均处于 `uptrend`，但两者都有 `symbol_risk_veto`，且 `0006.HK` 还不是主题 leader，因此只能作为防御主题观察，不可升级。
2. **telecom-dividend**：主题均分 `75.00`，`0941.HK` 分数 `81.98` 高于 `0728.HK` 的 `68.01`；`0941.HK` 同时有 `uptrend` 与 `volume-expansion`，相对更强。但 `0941.HK` 被 `symbol_risk_veto` 阻断，`0728.HK` 也不是主题 leader 且同样有风险阻断，因此只列观察。
3. **financials-bank**：`0005.HK` 是单一主题成员，分数 `71.01`，是唯一进入 `actionable_candidates` 的标的；趋势结构完整，但因日期错配，只能列为条件审计项。
4. **energy**：主题均分 `69.67`。`0857.HK` 分数最高 `84.42`，但 `symbol_risk_veto` 很重；`0883.HK` 分数 `73.93` 也被 veto 且非 leader；`0386.HK` 分数较低 `50.67`，但无 `symbol_risk_veto` 且出现 `volume-expansion`，适合作为“干净替代标的”的观察对象，而非买入对象。
5. **financials-exchange**：`0388.HK` 分数 `67.30`，有 `volume-expansion`，但 `symbol_risk_veto` 阻断，且历史 pass_rate 不足，只能观察。

### 最弱主题
- **healthcare-pharma / healthcare-biotech**：`1177.HK`、`1093.HK`、`2269.HK` 均为低分或下行结构，多个标的处于 `downtrend`，暂不做多。
- **consumer-tech**：`1810.HK` 分数 `0`，`downtrend`，且历史低 pass_rate；不升级。
- **hang-seng-tech**：`3033.HK`、`3067.HK` 均低于 watch 分数门槛，且 MA60 未收复，科技反弹只能观察。
- **internet-platform**：虽然 `9618.HK` 个体分数 `72.27`，但主题均分仅 `32.34`，且多个互联网标的存在低 pass_rate、selection error 或 MA60 未收复；需要 ETF 与同主题相对强度确认。

### 当前交易宇宙未覆盖的机会
- 现有宇宙缺少直接跟踪**高股息/红利 ETF**、**恒生国企指数 ETF**、**黄金/商品 ETF**或**港股银行篮子 ETF**的工具。当前强势集中在防御、公用、能源、电信与银行，若要降低单股风险，后续应补充流动性充足的主题 ETF 用于确认与替代，而不是用单一股票机械表达主题。

## 市场状态与操作原则

- 市场雷达显示 `risk_on`，股票平均 1 日涨幅 `1.606%`，ETF 平均 1 日涨幅 `1.70%`。
- 但历史 replay 行情日期错配，且 posterior 评估显示大量 `symbol_selection_error`、`timing_unclear` 与低 pass_rate。今日计划以“确认清单”为主。
- 只有 `actionable_candidates` 中的 `0005.HK` 可被考虑为升级候选；所有 `diagnostic_candidates` 只能用于观察、排除或同主题比较。
- 成本门槛：往返成本估计 `35 bps`，最低边际收益要求 `100 bps`，预期 swing edge 必须清楚超过成本与最低边际收益门槛。

## ?????????（候选强弱排序）

1. **`0005.HK` HSBC / 汇丰控股 — 条件 `buy_candidate` 审计项，实际状态：`watch_only`**
2. **`0386.HK` Sinopec / 中国石油化工股份 — 能源替代观察，状态：`watch_only`**
3. **`0941.HK` China Mobile / 中国移动 — 强势但 veto 阻断，状态：`watch_only`**
4. **`0002.HK` CLP Holdings / 中电控股 — 防御主题观察，状态：`watch_only`**
5. **`0388.HK` HKEX / 香港交易所 — 放量金融弹性观察，状态：`watch_only`**

---

## 候选 1：`0005.HK` HSBC / 汇丰控股

- **主题**：financials-bank
- **今日状态**：条件 `buy_candidate` 审计项；实际执行状态维持 `watch_only`
- **关注理由**：唯一进入 `actionable_candidates` 的标的，`score=71.01`，`trend_score=96.41`，无 `symbol_risk_veto`，价格高于 MA20 与 MA60，且 MA20 高于 MA60。
- **同主题比较**：该主题在交易宇宙中只有 `0005.HK`，无直接同主题股票可比；因此需要用 `2800.HK`、`0388.HK`、`1299.HK` 及更广义金融/指数表现来间接确认，而不能只因单股技术形态升级。
- **已有证据**：
  - `latest_close=141.0`，高于 `ma20=137.425` 与 `ma60=133.1856`。
  - `range_pos_60=0.8805`，处于 60 日区间偏高位置，趋势仍强。
  - `qualification_flags` 包含 `score_meets_action_threshold`。
- **缺失证据（升级到 accumulate/buy 前必须补齐）**：
  - 日期对齐的 2026-01-09 附近行情确认，而不是 `2026-04-29` quote。
  - 放量确认：当前 `volume_ratio_20=0.7907`，未体现强量能，需要至少回到 1.0 以上并保持价格不破 MA20。
  - 金融板块同步确认：`0388.HK`、`1299.HK` 或大盘 ETF 至少一个同步站稳，不出现单股孤立上行。
  - 预期 14-90 日 swing edge 明确超过 `100 bps` 且超过成本两倍。
- **风险**：60 日区间位置偏高，若无量能跟随，容易变成防御拥挤交易；posterior 中 `0005.HK` 样本少且 avg_return 为负，历史证据不足。
- **失效条件**：收盘跌破 MA20，或金融同组明显转弱，或补齐日期对齐数据后发现当前价低于 MA60/趋势破坏。
- **时间窗口**：14-45 天。
- **信心**：低到中；因唯一 action list 标的而优先，但受日期错配与量能不足限制。

## 候选 2：`0386.HK` Sinopec / 中国石油化工股份

- **主题**：energy
- **今日状态**：`watch_only`
- **关注理由**：能源主题强，但 leader `0857.HK` 与次强 `0883.HK` 均被 `symbol_risk_veto` 阻断；`0386.HK` 虽分数较低，却没有 `symbol_risk_veto`，且 `volume_ratio_20=2.2023`，可作为“干净替代”的观察对象。
- **同主题比较**：
  - `0857.HK`：分数 `84.42`、趋势最强，但 pass_rate 低、avg_return 差、存在 repeated selection error，不能升级。
  - `0883.HK`：分数 `73.93`，趋势强，但同样被 veto，且非 leader。
  - `0386.HK`：分数 `50.67`，低于 action 门槛且非 leader；优势在于无 veto 与显著放量，但 MA60 尚未收复。
- **已有证据**：
  - `pct_change_1d=2.40%`，`volume_ratio_20=2.2023`，有短线资金关注。
  - 价格 `4.70` 高于 `ma20=4.5865`。
  - `regime_flags` 包含 `range` 与 `volume-expansion`。
- **缺失证据（升级到 accumulate/buy 前必须补齐）**：
  - 必须进入 `actionable_candidates`；当前不是 action list 标的。
  - 收复 `ma60=4.9833` 并至少连续 2 个交易日站稳。
  - 相对 `0857.HK` 与 `0883.HK` 出现 fresh peer-relative strength，而不是只因其他标的被 veto 被动替代。
  - 油价/能源板块或相关 ETF 的同步确认。
- **风险**：目前是 same-theme non-leader，不能替代 leader 直接升级；若放量后无法收复 MA60，可能只是区间反弹。
- **失效条件**：跌回 MA20 下方，或放量后 2-3 日内无法延续，或能源主题转弱。
- **时间窗口**：14-30 天观察。
- **信心**：低；适合作为能源主题的审计跟踪，而非买入。

## 候选 3：`0941.HK` China Mobile / 中国移动

- **主题**：telecom-dividend
- **今日状态**：`watch_only`
- **关注理由**：分数 `81.98`，`trend_score=100`，`volume_ratio_20=1.5041`，同时具备 `uptrend` 与 `volume-expansion`，是电信红利主题里最强形态。
- **同主题比较**：`0941.HK` 明显强于 `0728.HK`：前者分数 `81.98`，后者 `68.01`；前者 MA20 高于 MA60 且放量，后者虽然放量但不是主题 leader，并有 same-theme non-leader 限制。因此若只看技术结构，`0941.HK` 是更好表达。
- **已有证据**：
  - `latest_close=85.45`，高于 `ma20=81.55` 与 `ma60=79.82`。
  - `range_pos_60=1.1119`，强势突破 60 日区间上沿。
  - `volume_ratio_20=1.5041`，量能确认。
- **缺失证据（升级到 accumulate/buy 前必须补齐）**：
  - 解除或重新评估 `symbol_risk_veto`：历史 pass_rate 极低，且有 adverse breach 与 selection error。
  - 日期对齐行情确认，不可使用错配 quote 直接升级。
  - 回踩不破前高或 MA20 后再次放量上行，避免追高。
  - 与 `0728.HK` 的相对强度继续维持，而不是短期过热后轮动。
- **风险**：range_pos_60 已超过 1，存在突破后回撤风险；posterior 记录显示该 symbol 历史调用质量差。
- **失效条件**：跌回前突破区间内并失守 MA20，或 `0728.HK` 明显转强而 `0941.HK` 滞涨。
- **时间窗口**：14-45 天观察。
- **信心**：中等技术强度，低操作信心。

## 候选 4：`0002.HK` CLP Holdings / 中电控股

- **主题**：utilities-defensive
- **今日状态**：`watch_only`
- **关注理由**：防御主题均分最高，`0002.HK` 是主题 leader，分数 `76.08`，趋势结构稳定，适合作为风险偏好降温时的防御候选。
- **同主题比较**：`0002.HK` 分数 `76.08` 略高于 `0006.HK` 的 `74.86`，且是 theme leader。`0006.HK` 当日小跌 `-0.38%`，同时非 leader 并有 `not_theme_score_leader` 限制，因此当前 `0002.HK` 更适合作为主题观察代表。
- **已有证据**：
  - `latest_close=75.85`，高于 `ma20=74.5825` 与 `ma60=73.9937`。
  - `volume_ratio_20=1.2769`，量能尚可。
  - `regime_flags` 为 `uptrend`。
- **缺失证据（升级到 accumulate/buy 前必须补齐）**：
  - 必须解除 `symbol_risk_veto`；当前 pass_rate 为 0 且 avg_return 为负。
  - 确认不是 risk_on 日里的防御补涨尾端，需要观察大盘回撤时是否仍有相对强度。
  - 日期对齐行情与 3-5 日 follow-through。
  - 同主题中继续优于 `0006.HK`，而非短期 leader 互换。
- **风险**：防御股在 risk_on 日可能落后成长/周期；若利率或公用股估值压力上升，趋势可能快速钝化。
- **失效条件**：跌破 MA20，或 `0006.HK` 连续跑赢并取代其主题 leader 地位，或防御主题均分跌破 watch 门槛。
- **时间窗口**：14-60 天观察。
- **信心**：低；主题强但 veto 阻断。

## 候选 5：`0388.HK` HKEX / 香港交易所

- **主题**：financials-exchange
- **今日状态**：`watch_only`
- **关注理由**：当日涨幅 `2.99%`，`volume_ratio_20=1.5564`，出现 `volume-expansion`，是金融弹性方向的重要观察标的。
- **同主题比较**：该主题在交易宇宙中只有 `0388.HK`。与广义金融标的比较，`0388.HK` 的量能优于 `0005.HK`，但 `0005.HK` 是唯一 action list 标的且无 veto；`0388.HK` 由于 `symbol_risk_veto`，只能作为金融风险偏好确认工具。
- **已有证据**：
  - `latest_close=419.8`，高于 `ma20=406.12` 与 `ma60=406.7173`。
  - `pct_change_1d=2.99%`，量价同步改善。
  - `range_pos_60=0.6766`，仍未极端过热。
- **缺失证据（升级到 accumulate/buy 前必须补齐）**：
  - 必须解除 `symbol_risk_veto`，当前 pass_rate 为 0。
  - 需要港股成交额、IPO/南向资金等交易活跃度证据支持，而不是单日放量。
  - 需要后续站稳 MA60 上方并延续相对 `0005.HK`、`1299.HK` 的强势。
  - 必须进入 `actionable_candidates` 后才可考虑升级。
- **风险**：交易所弹性高，若大盘反弹无持续成交，容易回落；posterior 样本少且未通过风险门槛。
- **失效条件**：跌回 MA60 下方，或放量阳线后无连续成交额支持，或金融主题转弱。
- **时间窗口**：14-45 天观察。
- **信心**：低到中；作为确认指标价值高，作为交易标的暂低。

## Disqualifiers

以下情况强制维持 `watch_only` 或 `avoid`，不得升级：

1. **日期错配**：`as_of_date=2026-01-09`，但 quote 为 `2026-04-29`；所有信号必须降级为条件审计。
2. **非 action list 标的**：除 `0005.HK` 外，其他标的不在 `actionable_candidates` 中；`diagnostic_candidates` 只能用于观察或排除。
3. **`symbol_risk_veto`**：`0857.HK`、`0941.HK`、`0002.HK`、`0388.HK` 等被 veto 的标的不得升级，即使分数高。
4. **same-theme non-leader**：`0386.HK`、`0883.HK`、`0728.HK`、`0006.HK` 等若不是主题 leader，必须证明 fresh peer-relative strength，不能因 leader 被 veto 而机械替代。
5. **弱科技/互联网 ETF 确认不足**：`3033.HK`、`3067.HK` 低于 watch 分数且 MA60 未收复；互联网单股即使日内反弹，也需要 ETF 与 peer-relative 确认。
6. **低 pass_rate 与 recent misfires**：`2800.HK`、`9988.HK`、`0700.HK`、`3033.HK`、`3067.HK` 等有近期失败或 selection error，不能仅凭单日上涨升级。
7. **量能不足**：高趋势分若 `volume_ratio_20` 未达确认要求，尤其是 `0005.HK` 当前 `0.7907`，只能列为条件观察。

## 今日研究任务清单

1. 补齐与 `2026-01-09` 对齐的真实收盘、成交量、MA20、MA60 数据，优先校验 `0005.HK`、`0386.HK`、`0941.HK`、`0002.HK`、`0388.HK`。
2. 对 `0005.HK` 做金融组确认：比较 `0388.HK`、`1299.HK`、`2800.HK` 的同步强度与成交量。
3. 对能源主题做替代审计：检查 `0386.HK` 是否能在后续收复 MA60，并相对 `0857.HK`、`0883.HK` 改善。
4. 对电信与公用主题只做观察，不做升级，重点跟踪 veto 标的是否只是历史模型误伤还是仍有现实风险。
5. 不更新长期记忆；本次是 historical bootstrap replay，当前输入已显示重复模式，但 active learnings 已覆盖主要约束，暂无新的稳定规则需要添加。

## 结论

今日最强的可审计候选是 `0005.HK`，但由于 replay 日期错配、量能不足以及 posterior 样本薄弱，实际状态仍为 `watch_only`。其余高分标的大多被 `symbol_risk_veto` 或同主题替代规则阻断。今日计划的核心不是追买强势，而是验证：银行是否能成为低风险金融表达、能源是否有无 veto 的替代表达、以及防御/电信高分是否能摆脱历史低 pass_rate 的约束。
