# 2025-12-23 每日研究计划（historical）

> 模式：`recommendation_only`  
> 时间窗口：14-90 天 swing  
> 重要限制：本次 `as_of_date` 为 `2025-12-23`，但行情字段 `quote_trade_date` 为 `2026-04-28`。因此所有结论只作为历史回放中的诊断计划；在缺少日期对齐证据前，不能升级到 `accumulate` / `buy_candidate` / `hold`。  
> 组合状态：未提供真实持仓；`2800.HK` 只是 watchlist placeholder，不作为实盘持仓管理。

## 市场扫描

### 最强主题

1. **`utilities-defensive`**：主题均分 72.44，`0006.HK` 与 `0002.HK` 都处于 `uptrend`，防御属性在 `risk_off` 环境中相对占优。问题是主题 leader `0006.HK` 被 `symbol_risk_veto` 阻断，替代 peer `0002.HK` 需要独立成交量与相对强度确认。
2. **`telecom-dividend`**：主题均分 67.53，`0941.HK` 趋势最强但被 `symbol_risk_veto` 阻断；`0728.HK` 成交量确认更好，但仍是 `range`，且 MA20 尚低于 MA60。
3. **`energy`**：主题均分 56.81，油气股是当日少数上行方向。`0857.HK` 分数最高但被 `symbol_risk_veto` 阻断；`0883.HK` 没有 symbol veto，趋势结构较干净，但不是主题分数 leader，且成交量不足 1.0。

### 最弱主题

1. **`hang-seng-tech` / `hong-kong-broad-market` ETF**：`3033.HK`、`3067.HK`、`2800.HK` 均为 `downtrend` 且分数为 0，不能提供 broad/ETF confirmation。
2. **`healthcare-pharma` 与 `healthcare-biotech`**：`1093.HK`、`1177.HK` 均为 `downtrend`；`2269.HK` 虽有 `volume-expansion`，但仍低于 MA20/MA60。
3. **`consumer-tech`**：`1810.HK` 单日 -3.79%，`downtrend`，且 range_pos_60 为 -0.1149，是高反弹风险与下行结构并存的名字，只能观察，不能主动做空或 `avoid` 升级。

### 当前交易宇宙未覆盖的机会

- `risk_off` 环境下，交易宇宙缺少更直接的防御/避险表达：例如港股高股息 ETF、短久期债券/货币市场 ETF、黄金相关 ETF 或能源上游以外的低 beta 收益资产。当前只能在公用事业、电讯、能源股内做相对选择，无法用更纯粹的避险工具验证风险偏好。

## 核心结论

- 今天没有可升级标的：`actionable_candidates` 为空，按规则阻断所有 `accumulate` / `buy_candidate` / `hold`。
- 排名较高的 `diagnostic_candidates`（`0857.HK`、`0941.HK`、`0006.HK`）只能作为 watch/peer-audit 线索，因为全部有 `symbol_risk_veto`。
- 可关注的替代 peer（`0883.HK`、`0002.HK`、`0728.HK`）也不能自动接棒；它们必须独立满足趋势、成交量、相对强度与风险门槛。
- 基准 ETF 与科技 ETF 均未确认，市场整体仍按 `risk_off` 处理。

## 候选排序

| 排名 | 标的 | 主题 | 今日状态 | 为什么进入计划 | 不能升级的关键原因 |
|---:|---|---|---|---|---|
| 1 | `0883.HK` | `energy` | `watch_only` | 油气主题中无 `symbol_risk_veto` 的较强 peer，价格在 MA20/MA60 之上，单日 +1.90% | 非主题分数 leader，`volume_ratio_20` 仅 0.8064，`actionable_candidates` 为空 |
| 2 | `0002.HK` | `utilities-defensive` | `watch_only` | 防御主题强，价格在 MA20/MA60 之上，无 `symbol_risk_veto` | 非主题 leader，成交量偏弱，需证明强于 `0006.HK` |
| 3 | `0728.HK` | `telecom-dividend` | `watch_only` | 成交量确认最好，`volume_ratio_20` 1.3878，电讯防御主题仍强 | 仍为 `range`，MA20 < MA60，非主题 leader |
| 4 | `9618.HK` | `internet-platform` | `watch_only` | 互联网平台中唯一 `uptrend` leader，价格在 MA20/MA60 之上 | 主题均分弱、ETF 不确认、低于 action score 且有 `symbol_risk_veto` |
| 5 | `2269.HK` | `healthcare-biotech` | `watch_only` | 下跌主题中出现放量反弹信号，适合跟踪是否转强 | `downtrend`，价格低于 MA20/MA60，只能作为反转观察 |

## 强主题内 peer 对比

### `energy`：优先观察 `0883.HK`，而不是机械选择 `0857.HK`

- `0857.HK`：score 81.90，主题 leader，趋势和成交量最强；但 `symbol_risk_veto` 明确，历史 pass_rate 0.067，平均回报 -15.736%，且有 repeated `symbol_selection_error`。
- `0883.HK`：score 72.45，`uptrend`，latest_close 28.98 高于 MA20 27.27 与 MA60 26.5997；无 `symbol_risk_veto`，是更干净的观察对象。
- `0386.HK`：score 16.09，`range`，MA60 上方未恢复，range_pos_60 仅 0.0932，暂不纳入候选。

**选择理由**：`0883.HK` 不是分数 leader，但在 leader 被 veto 时，它是能源主题中唯一具备趋势结构且没有历史风险阻断的 peer。今天只观察，不升级。

**升级前缺失证据**：
- 日期对齐后的行情确认仍在 MA20/MA60 上方。
- `volume_ratio_20` 回到或高于 1.0，并最好高于 `0857.HK` 的相对强度表现。
- 能源主题继续领先，而不是单日脉冲。
- `actionable_candidates` 出现，且成本门槛下预期 swing edge 明显超过 100 bps 与往返成本 35 bps。

### `utilities-defensive`：优先观察 `0002.HK`，但需证明强于 `0006.HK`

- `0006.HK`：score 75.51，主题 leader，`uptrend`，成交量确认；但 `symbol_risk_veto`，历史 pass_rate 0.000，平均回报 -7.524%。
- `0002.HK`：score 69.37，`uptrend`，latest_close 75.5 高于 MA20 74.4525 与 MA60 73.9485；无 `symbol_risk_veto`。

**选择理由**：公用事业是当前最强主题，`0002.HK` 是无 veto 的替代防御表达。它比 `0006.HK` 弱在成交量和 leader 地位，所以只能作为 watchlist 第一层防御候选。

**升级前缺失证据**：
- `0002.HK` 成交量改善，`volume_ratio_20` 至少回到 1.0 附近。
- 连续数日相对收益不弱于 `0006.HK`，证明替代 peer 不是被动跟随。
- 价格守住 MA20，且 MA20 > MA60 继续扩大。
- broad market 没有进一步加速下跌；否则防御股也可能被流动性拖累。

### `telecom-dividend`：观察 `0728.HK` 的补涨确认，不追 `0941.HK`

- `0941.HK`：score 76.46，主题 leader，`uptrend`，成交量确认；但 `symbol_risk_veto`，历史 pass_rate 0.071。
- `0728.HK`：score 58.59，`range`，latest_close 5.17 高于 MA20 4.948 与 MA60 5.007，`volume_ratio_20` 1.3878，成交量优于 `0941.HK`。

**选择理由**：`0728.HK` 的成交量最有观察价值，但它还没有完成趋势确认。若它能把 MA20 拉回 MA60 上方并维持放量，才可能成为比被 veto 的 `0941.HK` 更合格的电讯表达。

**升级前缺失证据**：
- MA20 上穿 MA60，或至少连续收盘维持在 MA60 上方。
- 相对 `0941.HK` 的强度改善，而不是只靠低位补涨。
- 电讯主题继续保持强于大市与科技 ETF。
- 进入 `actionable_candidates`，并清除“非主题 leader”的替代风险。

## 其他候选说明

### `9618.HK` JD.com / 京东集团-SW — `watch_only`

**为什么今天值得看**：在 `internet-platform` 中，`9618.HK` 是唯一明显 `uptrend` 的 leader，latest_close 115.3 高于 MA20 115.2223 与 MA60 107.8539，说明互联网内部存在少数相对强点。

**为什么不升级**：主题均分只有 16.40，`0700.HK`、`9988.HK`、`1024.HK` 多数仍为 `downtrend`；`3033.HK` / `3067.HK` 也未确认。`9618.HK` 本身低于 action score，且有 `symbol_risk_veto`。

**升级前缺失证据**：
- `3033.HK` / `3067.HK` 至少重回 MA20，并最好重新站上 MA60。
- `9618.HK` 成交量放大至 `volume_ratio_20` > 1.0。
- 互联网平台 peer breadth 改善，不能只有单名独强。
- `symbol_risk_veto` 风险解除或被新的 posterior 证据显著修正。

### `2269.HK` WuXi Biologics / 药明生物 — `watch_only`

**为什么今天值得看**：虽然仍是 `downtrend`，但 `pct_change_1d` +1.26% 且 `volume_ratio_20` 1.5616，说明弱主题中出现放量尝试。它适合观察是否形成反转，而不是今天行动。

**为什么不升级**：价格仍低于 MA20 34.688 与 MA60 36.4207，主题分数为 0，且 healthcare 整体偏弱；放量可能只是下跌趋势中的短线反抽。

**升级前缺失证据**：
- 先收复 MA20，再观察是否挑战 MA60。
- 放量上涨需要连续性，不能只靠单日 volume-expansion。
- healthcare-pharma 与 healthcare-biotech 内部不再同步走弱。
- broad market 风险状态从 `risk_off` 至少改善到中性。

## Disqualifiers

以下条件强制维持 `watch_only` 或回避升级：

1. **`actionable_candidates` 为空**：所有 diagnostic names 都不能升级到 `accumulate` / `buy_candidate` / `hold`。
2. **日期错配**：`as_of_date` 为 `2025-12-23`，但 `quote_trade_date` 为 `2026-04-28`；缺少独立日期对齐证据前，所有信号只用于诊断。
3. **`risk_off` 市场状态**：平均股票 1 日 -0.946%，ETF 平均 -1.847%，不支持追逐动量。
4. **ETF 不确认**：`2800.HK`、`3033.HK`、`3067.HK` 均为 `downtrend`，无法支持 broad-market 或 tech-beta 升级。
5. **top diagnostic leaders 被 veto**：`0857.HK`、`0941.HK`、`0006.HK` 均有 `symbol_risk_veto`；它们只能用于 peer audit，不能作为行动标的。
6. **替代 peer 不能机械接棒**：`0883.HK`、`0002.HK`、`0728.HK` 虽无相同 veto，但分别存在成交量不足、非 leader、趋势未完成的问题。
7. **低 pass-rate 历史约束**：`2800.HK`、`3033.HK`、`3067.HK`、`0700.HK`、`9988.HK` 等低 pass-rate 名字不得因单日反弹升级。
8. **成本门槛**：任何候选必须显示预期 swing edge > 100 bps 且明显超过 35 bps 往返成本；今天没有证据满足。

## 今日执行计划

1. **先不行动**：维持 recommendation-only 的 `watch_only` 输出，不模拟真实持仓调整。
2. **建立 peer-audit 队列**：重点跟踪 `0883.HK` vs `0857.HK`、`0002.HK` vs `0006.HK`、`0728.HK` vs `0941.HK`。
3. **等待 ETF 确认**：若 `2800.HK` 与 `3033.HK` / `3067.HK` 继续低于 MA20/MA60，则所有 beta 与科技相关候选继续压低置信度。
4. **记录缺失证据**：下一次只在日期对齐、成交量改善、peer-relative improvement 与 `actionable_candidates` 同时改善时，才考虑从 `watch_only` 升级。

## 最终计划摘要

- 最强观察：`0883.HK`，因为能源主题强且没有 `symbol_risk_veto`，但成交量不足、非 leader，仍是 `watch_only`。
- 最稳防御观察：`0002.HK`，但必须证明相对 `0006.HK` 的 peer improvement。
- 最值得等待确认：`0728.HK`，成交量强，但趋势结构尚未完成。
- 互联网只看不买：`9618.HK` 是内部强者，但 ETF 与主题 breadth 不支持升级。
- 反转观察：`2269.HK` 有放量，但趋势仍下行，不能把反弹当反转。

结论：今天没有行动候选。小章鱼先把触手收回来，盯住强主题里的替代 peer，等真正日期对齐、ETF 确认、成交量和 peer-relative 证据一起出现。