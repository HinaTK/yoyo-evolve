# 2025-12-18 投资复盘反思

> 会话：`historical`  
> 时间：2025-12-18 20:29  
> 模式：recommendation_only / 历史回放  
> 结论先行：今天最重要的动作不是找买点，而是承认没有买点。`actionable_candidates=[]`、`risk_state=risk_off`、ETF 弱于股票、多个高分主题 leader 被 `symbol_risk_veto` 阻断，再加上 `as_of_date` 与报价日期不一致，足够把所有建议压到 `watch_only`。

## 这次判断哪里最脆弱

我今天最弱的置信点有四个：

1. **日期一致性最弱。** 快照标注 `as_of_date=2025-12-18`，但行情字段显示 `quote_trade_date=2026-04-28`。这意味着排名只能作为结构诊断，不能当作当日实时确认。任何看起来漂亮的分数，都可能只是错位数据上的漂亮。
2. **主题强度与可交易性之间断裂。** `utilities-defensive`、`telecom-dividend`、`energy` 的主题分数靠前，但各自的主题 leader `0006.HK`、`0941.HK`、`0857.HK` 都被 `symbol_risk_veto` 拦住。主题强，不等于有可交易表达。
3. **替代标的证据不足。** `0883.HK`、`0002.HK`、`0728.HK` 比被 veto 的 leader 更干净，但它们不是模型主题分数 leader，且不是所有项都同时满足成交、相对强度、风险和行动阈值。不能因为第一名不能买，就自动买第二名。
4. **互联网平台的相对强度仍未被 ETF 证实。** `9618.HK` 是互联网平台主题内相对最干净的观察对象，但恒生科技 ETF `3033.HK`、`3067.HK` 都在 `downtrend`，宽基 `2800.HK` 也低于 MA20/MA60。单股结构不能替代主题确认。

## 仍然缺失的证据

- 缺少日期对齐的真实 2025-12-18 行情确认。
- 缺少宽基和科技 ETF 的 MA20/MA60 修复证据。
- 缺少 `actionable_candidates` 重新出现的策略层确认。
- 缺少被 veto 标的风险解除或后验改善证据。
- 缺少同主题替代 peer 的明确相对强度证据，例如 `0883.HK` 是否持续强于 `0857.HK`，`0002.HK` 是否持续强于 `0006.HK`，`0728.HK` 是否持续强于 `0941.HK`。
- 缺少成本门槛后的预期边际收益：当前 `estimated_round_trip_bps=35.0`、`minimum_edge_bps=100.0`，但没有任何候选能清楚证明 swing edge 足够覆盖两道门槛。

## 今日建议的失败模式

今天没有升级为 `buy_candidate`、`accumulate` 或 `hold`，所以主要失败模式不是“买错”，而是观察框架误导下一轮：

1. **symbol-selection error：** 动态观察 `9618.HK` 可能只是互联网弱主题里的相对强，而不是未来最佳表达。如果 `1024.HK`、`9988.HK` 或其他同主题 peer 在后续窗口明显跑赢，而我仍固守 `9618.HK`，就是选股错误。
2. **theme error：** 防御/能源主题今天看起来抗跌，但如果后续市场切回科技/互联网反弹，今天对防御和能源的关注会变成主题滞后。
3. **timing error：** `0883.HK`、`0002.HK`、`0728.HK` 这类“干净但非 leader”的替代标的可能短期继续强，但由于规则没有允许升级，我可能错过早段收益；不过在当前证据质量下，这种错过比绕过 veto 更可接受。

## 动态选择分类

- `9618.HK`：动态选择为互联网平台主题观察对象。若未来失败，优先归类为 **symbol-selection error**，其次是 **theme error**；因为互联网整体仍弱，ETF 未确认。
- `0857.HK`：排名系统给出最高诊断分，但被 `symbol_risk_veto` 阻断。若未来它继续上涨，不能直接归咎为模型错过；更可能是 **risk-control error 的机会成本**，但当前 veto 有后验证据支持。
- `0883.HK`：作为能源主题内更干净的替代观察项。若后续跑赢 `0857.HK`，说明规则需要继续精化“leader 被 veto 后如何评估替代 peer”；若跑输，则今天没有升级是正确的。

## 下一轮优先级调整

1. **先检查日期一致性。** 如果 `as_of_date` 与报价日期仍冲突，继续只写诊断，不写行动建议。
2. **先看 `actionable_candidates`，再看分数。** 只要行动层为空，所有高分诊断候选都只能变成观察条件。
3. **对被 veto 的主题 leader 建立替代 peer 表。** 每个强主题至少比较 leader 与第二名：趋势、成交、MA20/MA60、60 日区间位置、后验风险、近 3-5 日相对表现。
4. **互联网平台只保留 `9618.HK` 为观察，不升级。** 触发升级前必须看到 `3033.HK` 或 `3067.HK` 收复 MA20，最好进一步接近或收复 MA60，并且 `9618.HK` 放量站稳 MA20。
5. **能源主题重点验证 `0883.HK` 而不是绕过 `0857.HK` veto。** 如果 `0883.HK` 能持续强于 `0857.HK` 且成交改善，再讨论是否作为更干净表达；否则不行动。
6. **参数层保持保守。** 当前 champion 样本只有 `4`，`robust_score=-6.275`、`win_rate=0.0`，不足以支持放宽门槛；下一轮不应因为策略仍为 active 就误读为策略有效。

## 今日结论

今天的正确姿态是：**全部 `watch_only`，不追涨、不抄底、不绕过 veto。**

这不是胆小，是规则终于在帮我少犯旧错：后验里 `symbol_selection_error=100`，`theme_error=43`，`timing_unclear=34`。当历史已经反复告诉我“高分不等于好交易、主题 leader 不等于最佳表达、后验不能当实时确认”，今天最有价值的动作就是让这些教训真的约束我。
