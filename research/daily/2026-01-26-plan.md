# 2026-01-26 每日计划（historical replay）

## 市场雷达

**结论先行：本日不产生 `buy_candidate` / `accumulate` 升级。** 当前 `actionable_candidates=[]`，且排名输入的 `as_of_date=2026-01-26` 与报价字段 `quote_trade_date=2026-04-30` 不对齐；按稳定规则，这些信号只能作为历史回放中的审计线索，不能作为当日行动触发。

### 最强主题

1. **utilities-defensive**：主题均分 `54.38`，为当前最高；`0002.HK` 与 `0006.HK` 均处于 `uptrend`、价格高于 ma20/ma60，但二者都被 `low_volume_ratio_20_below_0_6` 与 `symbol_risk_veto` 限制，只能做防御主题审计。
2. **energy**：`0883.HK`、`0857.HK` 趋势分很高，价格均在 ma20/ma60 上方；但主题领头 `0883.HK` 和次强 `0857.HK` 均有 `symbol_risk_veto`，且能源主题内替代不允许绕过 veto。
3. **telecom-dividend**：`0941.HK` 是主题领头，趋势结构强；但成交量比率极低且历史 pass_rate 为 0，不能升级。
4. **financials-exchange / financials-bank**：`0388.HK`、`0005.HK` 均为各自单一主题代表，价格结构偏强，但仍低于行动分数并触发风险 veto。

### 最弱主题

- **consumer-tech、healthcare-biotech、healthcare-pharma**：主题均分为 `0.0`，多个标的处于 `downtrend` 或价格低于 ma20/ma60，今日不进入候选。
- **hang-seng-tech**：ETF 主题均分 `9.82`，`3033.HK` 与 `3067.HK` 均未达到 watch 分数，且缺乏 ETF 确认；不能用作科技主题升级依据。
- **internet-platform**：主题均分 `14.91`，虽然 `9618.HK` 个体得分 `50.86`，但该主题近期存在大量 selected-vs-best 错误；`0700.HK`、`9988.HK`、`1024.HK` 等不能仅凭反弹想象升级。

### 当前交易宇宙之外的机会

当前交易宇宙没有覆盖**黄金、美元现金替代、短久期债券 ETF、南向高股息 ETF**等可能在防御/震荡环境中提供确认的品种。若后续扩展宇宙，应优先加入流动性高、可验证历史数据的 ETF，用于主题确认，而不是直接扩大单股风险。

## 候选排序

> 排序只代表今日研究优先级，不代表买入顺序。所有候选当前状态均为 `watch_only` 或审计项；只有进入 `actionable_candidates` 且同时通过成本、edge、风险与日期对齐检查后，才可考虑升级。

| 排名 | symbol | 主题 | 当前状态 | 关注理由 | 主要限制 |
|---:|---|---|---|---|---|
| 1 | `0883.HK` | energy | `watch_only` / audit | 全市场个体得分最高 `56.90`，趋势分 `96.95`，能源主题领头 | `symbol_risk_veto`、成交量比率低、低于行动分数、报价日期不对齐 |
| 2 | `0941.HK` | telecom-dividend | `watch_only` / audit | 电讯高息主题领头，趋势分 `99.06`，价格高于 ma20/ma60 | `symbol_risk_veto`、历史 pass_rate=0、成交量比率低 |
| 3 | `0002.HK` | utilities-defensive | `watch_only` / audit | 最强主题的领头，防御属性清晰，价格结构稳定 | `symbol_risk_veto`、低于行动分数、成交量比率低 |
| 4 | `0388.HK` | financials-exchange | `watch_only` / audit | 1日涨幅 `1.10%`，价格高于 ma20/ma60，交易所 beta 可跟踪市场情绪 | `symbol_risk_veto`、低于行动分数、成交量比率低 |
| 5 | `0005.HK` | financials-bank | `watch_only` / audit | 银行主题结构偏强，价格高于 ma20/ma60，趋势分 `95.46` | `symbol_risk_veto`、历史平均回报为负、成交量比率低 |

## 主题内比较与候选理由

### 1. `0883.HK`（CNOOC / 中国海洋石油）— energy

- **今日状态**：`watch_only` / audit。
- **为什么关注**：在能源主题内，`0883.HK` 得分 `56.90`，高于 `0857.HK` 的 `55.82` 与 `0386.HK` 的 `20.48`；同时是 `theme_leader`，价格 `29.76` 高于 ma20 `27.274` 与 ma60 `26.7913`，ma20 高于 ma60，结构上最完整。
- **同主题比较**：
  - 相比 `0857.HK`：`0857.HK` 趋势分更高 `99.70`，但不是主题得分领头，且同样有 `symbol_risk_veto`；不能作为绕过 `0883.HK` veto 的替代。
  - 相比 `0386.HK`：`0386.HK` 得分仅 `20.48`，价格仍低于 ma60，主题表达弱。
- **升级前缺失证据**：
  1. 必须出现在 `actionable_candidates`。
  2. 需要 date-aligned 报价与历史均线，不可用 2026-04-30 报价推断 2026-01-26 行动。
  3. 需要成交量比率恢复到可接受区间，解除 `low_volume_ratio_20_below_0_6`。
  4. 需要 posterior symbol risk 不再 veto，尤其是修复 `pass_rate=0.000 over 5 evaluated calls` 与平均回报负的问题。
  5. 需要相对 `0857.HK` 和 `0386.HK` 的新鲜强度确认，而不是只看绝对趋势。
- **失效条件**：跌回 ma20 下方并放量，或能源主题领头切换至 `0857.HK` 且 `0883.HK` 相对强度连续走弱。
- **时间窗口**：14–90 天，仅用于观察。
- **信心**：低到中；趋势强，但风控 veto 明确。

### 2. `0941.HK`（China Mobile / 中国移动）— telecom-dividend

- **今日状态**：`watch_only` / audit。
- **为什么关注**：`0941.HK` 为 telecom-dividend 主题领头，得分 `55.11`，显著高于同主题 `0728.HK` 的 `37.41`；价格 `85.2` 高于 ma20 `81.92` 与 ma60 `79.9358`，ma20 高于 ma60。
- **同主题比较**：
  - 相比 `0728.HK`：`0941.HK` 趋势、得分、主题排名均更强；`0728.HK` 未达 watch 分数，且不是主题领头。
  - 但 `0941.HK` 本身仍被 `symbol_risk_veto` 限制，因此强于同业不等于可行动。
- **升级前缺失证据**：
  1. 必须进入 `actionable_candidates`。
  2. 需要 date-aligned 价格、成交量与均线。
  3. 需要 `volume_ratio_20` 明显改善，当前仅 `0.0138`。
  4. 需要证明电讯高息主题不只是防御拥挤交易，且 T+3/T+5 后验不再持续 pass_rate=0。
  5. 需要明确收益率/分红预期或政策催化证据。
- **失效条件**：跌破 ma20，或 `0728.HK` 出现更强相对表现但主题整体未改善。
- **时间窗口**：14–90 天，仅用于观察。
- **信心**：中等偏低；结构强但历史表现 veto。

### 3. `0002.HK`（CLP Holdings / 中电控股）— utilities-defensive

- **今日状态**：`watch_only` / audit。
- **为什么关注**：utilities-defensive 是主题均分最高板块，`0002.HK` 为主题领头，得分 `54.49`，略高于 `0006.HK` 的 `54.26`；价格 `75.8` 高于 ma20 `74.73` 与 ma60 `74.0388`。
- **同主题比较**：
  - 相比 `0006.HK`：二者结构接近，但 `0002.HK` 是主题领头，且得分略高。
  - `0006.HK` 虽成交量比率 `0.1774` 高于 `0002.HK` 的 `0.0915`，但仍低于最低流动性确认要求，并且不是主题得分领头。
- **升级前缺失证据**：
  1. 必须出现在 `actionable_candidates`。
  2. 需要 date-aligned 的防御主题确认，而不是非当日报价。
  3. 需要解除 `symbol_risk_veto`；当前 `pass_rate=0.000 over 3 evaluated calls`。
  4. 需要收益率、利率敏感性、监管/燃料成本等基本面证据。
  5. 需要主题 ETF 或高股息/公用事业代理品种确认。
- **失效条件**：价格跌破 ma60，或 `0006.HK` 在相同日期窗口内持续跑赢且 `0002.HK` 量价转弱。
- **时间窗口**：14–90 天，仅用于观察。
- **信心**：中等偏低；主题稳，但行动证据不足。

### 4. `0388.HK`（HKEX / 香港交易所）— financials-exchange

- **今日状态**：`watch_only` / audit。
- **为什么关注**：`0388.HK` 单日涨幅 `1.10%`，成交额高，价格 `424.4` 高于 ma20 `407.59` 与 ma60 `406.6493`，可作为港股风险偏好和成交活跃度代理。
- **同主题比较**：该主题在交易宇宙中只有 `0388.HK`，无法做同主题股票替代；因此必须使用 `2800.HK`、市场成交、南向资金或券商/交易所相关外部证据进行交叉验证。
- **升级前缺失证据**：
  1. 必须进入 `actionable_candidates`。
  2. 需要 date-aligned 报价与市场成交数据。
  3. 需要解除 `symbol_risk_veto`；当前 `pass_rate=0.000 over 4 evaluated calls`。
  4. 需要港股大盘与成交额同步改善，尤其 `2800.HK` 不能继续弱于 ma60。
  5. 需要确认涨幅不是单日噪音。
- **失效条件**：跌破 ma20，或市场成交/大盘确认失败。
- **时间窗口**：14–90 天，仅用于观察。
- **信心**：低到中；个体结构较好，但缺少同主题验证。

### 5. `0005.HK`（HSBC / 汇丰控股）— financials-bank

- **今日状态**：`watch_only` / audit。
- **为什么关注**：`0005.HK` 得分 `51.02`，趋势分 `95.46`，价格 `140.2` 高于 ma20 `138.235` 与 ma60 `133.3526`；银行主题具备防御与利率敏感双重属性。
- **同主题比较**：该主题在交易宇宙中只有 `0005.HK`，无法做同业替代；需要与 `1299.HK`、`0388.HK` 以及大盘金融条件交叉验证，而不是孤立升级。
- **升级前缺失证据**：
  1. 必须出现在 `actionable_candidates`。
  2. 需要 date-aligned 报价。
  3. 需要解除 `symbol_risk_veto`；当前 `pass_rate=0.000 over 6 evaluated calls`，平均回报 `-1.649%`。
  4. 需要利率、信用风险、美元/港元流动性、分红预期等基本面证据。
  5. 需要金融主题内部确认：`0388.HK`、`1299.HK` 或大盘金融代理不能同时转弱。
- **失效条件**：跌破 ma20 或金融主题内部出现明显分化下行。
- **时间窗口**：14–90 天，仅用于观察。
- **信心**：低；结构尚可，但历史后验表现弱。

## Disqualifiers

以下条件强制将本日所有候选限制为 `watch_only` / audit，或对弱势标的维持 `avoid`：

1. **`actionable_candidates=[]`**：没有任何标的满足行动候选门槛。
2. **非日期对齐报价**：输入 `as_of_date=2026-01-26`，但行情字段为 `quote_trade_date=2026-04-30`；历史回放中不能用未来/非对齐报价生成当日交易触发。
3. **`symbol_risk_veto` 普遍存在**：前列候选 `0883.HK`、`0857.HK`、`0941.HK`、`0002.HK`、`0388.HK`、`0005.HK` 等均被历史风控限制。
4. **低成交量确认**：多数高分标的均触发 `low_volume_ratio_20_below_0_6`，不满足流动性/参与度确认。
5. **低于行动分数**：最高分 `0883.HK` 仅 `56.90`，低于 `min_action_score=65`。
6. **不可用同主题替代绕过 veto**：`0857.HK` 不能因 `0883.HK` 被 veto 而自动替代；同主题替代必须独立进入 `actionable_candidates` 并显示新鲜相对强度。
7. **ETF确认不足**：`2800.HK` 得分 `31.25`，`3033.HK` 得分 `19.65`，`3067.HK` 得分 `0`；宽基和科技 ETF 均不能支持升级。
8. **后验评估不能当作当前确认**：posterior evaluation 只能用于风险约束与学习，不能作为当日买入证据。

## 今日研究任务

1. **审计能源主题**：比较 `0883.HK`、`0857.HK`、`0386.HK` 的 date-aligned 价格、成交量、ma20/ma60 与后验 T+3/T+5 表现，确认是否只是非对齐报价造成的假强势。
2. **审计防御主题**：对 `0002.HK` 与 `0006.HK` 做收益率、利率敏感性与相对强度复核；若仍无行动候选，继续 watch_only。
3. **复核高息电讯**：检查 `0941.HK` 相对 `0728.HK` 是否持续跑赢，重点看成交量是否恢复，而不是只看趋势分。
4. **金融代理确认**：观察 `0388.HK`、`0005.HK`、`1299.HK` 是否形成一致金融主题，而不是单点跳涨。
5. **弱势主题排除**：对 internet-platform、hang-seng-tech、healthcare、consumer-tech 维持避免升级，除非出现日期对齐的 ETF/同主题强度反转。

## 推荐状态摘要

- `0883.HK`：`watch_only` / audit
- `0941.HK`：`watch_only` / audit
- `0002.HK`：`watch_only` / audit
- `0388.HK`：`watch_only` / audit
- `0005.HK`：`watch_only` / audit

本日计划的核心不是寻找替代买点，而是建立历史基线：在 `actionable_candidates=[]`、报价非日期对齐、并且高分候选普遍触发 veto 时，严格保持 `watch_only`，只做主题和同业相对强度审计。
