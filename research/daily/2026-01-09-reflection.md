# 2026-01-09 投资复盘反思

## 本次结论

今天是 historical replay，不是实盘确认。输入里的市场状态显示为 `risk_on`，平均股票涨幅约 1.606%，ETF 平均涨幅约 1.7%，短线情绪偏修复；但本次最关键的约束是：`as_of_date=2026-01-09`，而快照内 `quote_trade_date=2026-04-29`。因此排名结果只能作为历史回放诊断和条件审计，不能当作 2026-01-09 当天可直接执行的交易信号。

动态排名给出的唯一 `actionable_candidates` 是 `0005.HK`，分数 71.01，趋势分 96.41，价格高于 MA20/MA60，MA20 高于 MA60，且 `symbol_risk_veto=false`。若只看排序，`0005.HK` 是今天最干净的候选；但后验统计显示 `0005.HK` 样本数为 2、avg_return=-8.917%、pass_rate=0.0，说明风险引擎没有 veto 并不等于历史风险已经消失。叠加报价日期不对齐，本次不能把它升级为直接 `buy_candidate`，只能列为低置信条件观察：需要日期对齐数据确认趋势仍在、成交没有衰减、且相对同类金融/银行或大盘仍有强势，才允许进入下一轮行动评估。

## 信心最弱的位置

1. **日期一致性最弱**：本次所有报价字段均来自 `2026-04-29`，与 `2026-01-09` 回放日期冲突。任何看似 actionable 的信号都可能是未来数据污染后的排序产物。
2. **`0005.HK` 的风险识别最弱**：排名里 `symbol_risk_veto=false`，但后验摘要显示该标的已有 2 个样本、pass_rate=0.0、平均回报明显为负。样本不多，但亏损幅度足以要求人工风险降级。
3. **动态选股的同主题比较仍弱**：今天能源、通信、公用事业等主题分数高，但核心 leader（`0857.HK`、`0941.HK`、`0002.HK`）均被 `symbol_risk_veto` 拦截。不能因为主题强就自动替换到同主题第二名，也不能因为唯一可行动标的是 `0005.HK` 就忽视其后验风险。
4. **短线 timing 信心弱**：后验中 `timing_unclear=76`，近期 `2800.HK`、`9988.HK`、科技 ETF 的 T+3/T+5 失误仍多，说明风险偏好修复日很容易出现“看对方向、买错窗口”。

## 仍缺少的证据

- `2026-01-09` 日期对齐的真实 OHLCV、成交额、MA20/MA60、range_pos_60 和成交量比率。
- 日期对齐的 ETF/宽基确认：`2800.HK`、`3033.HK`、`3067.HK` 是否同步站上关键均线并放量。
- `0005.HK` 的同行或主题确认：银行/金融板块是否同步走强，而不是单一防御/高息股孤立强势。
- `0005.HK` 后验风险的人工复核：为什么风险模型没有 veto，而 posterior summary 已显示低 pass_rate 和较大负平均回报。
- 同主题 peer-relative 数据：若能源、通信或公用事业 leader 被 veto，替代 peer 是否真的相对被 veto leader 和最近 best peer 变强。

## 今日建议的可能失败模式

1. **risk-control error**：把非日期对齐的 `actionable_candidates` 当成实盘可执行信号，尤其是直接升级 `0005.HK`。
2. **symbol-selection error**：动态排序选择了唯一无 veto 的 `0005.HK`，但忽略 posterior summary 中该标的 pass_rate=0.0、avg_return=-8.917% 的人工风险提示。
3. **timing error**：`risk_on` 日的尾段修复可能只是短线反弹；若次日缺乏成交和宽基确认，T+3/T+5 可能再次失效。

若未来复盘发现 `0005.HK` 跑输金融同类或宽基，则更可能归类为 **symbol-selection error**；若全市场同步回落，则归类为 **timing error** 或 **theme error**；若错误来自忽视日期错配和后验风险，则是 **risk-control error**。

## 下一轮优先级调整

1. **先做日期门禁**：历史回放中只要 `as_of_date` 与 `quote_trade_date` 不一致，所有行动建议默认降为条件审计，不允许直接升级。
2. **补人工 posterior 风险门禁**：即使 `symbol_risk_veto=false`，若 posterior summary 显示 pass_rate 极低且平均回报显著为负，必须先降为 `watch_only` 或低置信条件观察。
3. **把同主题替代逻辑前置**：leader 被 veto 后，不自动买第二名；必须证明替代 peer 相对被 veto leader 和最近 best peer 都有新鲜强势。
4. **降低短线 timing 置信度**：对 `risk_on` 修复日的候选，先要求次日/后续成交与 MA 确认，再考虑从 `watch_only` 升级。
5. **继续约束低 pass_rate 标的**：`2800.HK`、`3033.HK`、`3067.HK`、`0700.HK`、`9988.HK`、`0857.HK`、`0941.HK` 等低通过率或 veto 标的只用于观察条件和错误归因，不作为主动升级来源。

## 操作记录

- `0005.HK`：条件观察，不作为直接行动。触发条件是日期对齐数据继续站上 MA20/MA60、成交不弱、金融/银行主题同步确认，并通过 posterior 风险复核。失效条件是跌回 MA20、相对宽基转弱，或后验风险复核继续显示负期望。
- `0857.HK`、`0941.HK`、`0002.HK`：仅诊断观察。尽管分数更高，但均被 `symbol_risk_veto` 拦截，不得升级。
- `2800.HK`、科技 ETF 与互联网平台：维持 `watch_only`。近期后验失败和 selected-vs-best 错误仍要求 ETF、成交、均线和 peer-relative 确认。
