# 2025-12-12 复盘反思

> Session: historical  
> 时间：2025-12-12 16:15  
> 模式：历史回放 / recommendation-only

## 结论摘要

今天的输入里没有独立的 market assessment、daily plan、daily report，只有交易宇宙快照、确定性排名和后验评价摘要。因此这份反思不能假装拥有完整的当日盘面叙事；它只能基于动态排名和长期后验约束，检查今天若要形成建议，哪里最容易错。

当前快照显示整体为 `risk_off`：平均股票单日跌幅约 -0.957%，ETF 平均跌幅约 -1.807%。强势集中在高股息/防御/能源：`0941.HK`、`0857.HK`、`0883.HK`、`0006.HK` 排名靠前；科技、互联网、医药和消费多数处于 `downtrend` 或弱势区间。确定性模型把 `0941.HK` 作为唯一 `actionable_candidates`，而 `0857.HK` 虽总分最高但被 `symbol_risk_veto` 拦截。

我的基准建议应是：

- `0941.HK`：可作为低仓位、条件型 `buy_candidate` / 优先观察对象，但必须设置回落失效条件；在 `risk_off` 环境里不应追高。
- `0006.HK`：虽达到行动分数，但未进入 `actionable_top_n`，更适合作为防御备选观察。
- `0857.HK`：只能保留为 diagnostic，不升级；后验记录显示 pass_rate=0.000、avg_return_pct=-14.335，并包含 `symbol_selection_error`。
- `0883.HK`：能源主题内价格结构良好，但因不是模型主题 leader，且能源 leader 本身被风险否决，只能作为“可能比 leader 更干净”的观察对象，不能自动替代升级。
- 科技/互联网/恒科 ETF：维持 `watch_only`，多数仍在 MA20/MA60 下方，且长期后验对 `0700.HK`、`9988.HK`、`3033.HK`、`3067.HK` 的低通过率约束仍然有效。

## 信心最弱的位置

1. **历史快照时间一致性最弱。** 输入的 `as_of_date` 是 `2025-12-12`，但报价字段显示 `2026-04-28`。这意味着我不能把价格、均线和当日涨跌当作真实 2025-12-12 盘后状态无条件使用。所有结论都应降一级置信度。
2. **`0941.HK` 的追高风险最弱。** 它分数高、趋势完整，但 `range_pos_60=1.0507`，已经接近或突破 60 日区间上沿。防御资产强势不等于有足够短线赔率。
3. **能源主题的标的选择信心最弱。** `0857.HK` 是主题 leader 但被后验风险否决；`0883.HK` 当日表现强、结构也好，却被 `not_theme_score_leader` 拦截。这里最容易发生“主题判断对、标的选择错”或“机械 leader 规则错杀更干净 peer”。
4. **短期窗口信心仍弱。** 后验摘要显示失败和 mixed 主要集中在 T+3/T+5 的早期窗口，不能用中期方向感覆盖短期入场时点。

## 仍然缺失的证据

- 缺少真实 2025-12-12 的即时市场评估、恒指/恒科指数位置、南向资金、行业 breadth 和成交结构。
- 缺少 `0941.HK`、`0006.HK`、`0857.HK`、`0883.HK` 在真实当日的新闻/基本面催化和是否存在除息、政策、油价等外部驱动。
- 缺少高股息/能源主题 ETF 或可比篮子的确认，无法判断这是主题性资金切换，还是单日防御拥挤。
- 缺少同主题 peer 在后续 T+3/T+5/T+10 的相对表现，用来验证“跳过风险否决 leader 后是否应选择第二名”。

## 今日建议的 1-3 个 likely failure modes

1. **timing error：** `0941.HK` 和 `0006.HK` 都处于高位强趋势，若短线资金从防御股切回超跌成长，低波动强势股可能先回撤，导致 T+3/T+5 表现不佳。
2. **symbol-selection error：** 能源主题可能继续强，但机械选择 `0857.HK` 会撞上历史风险否决；机械排除 `0883.HK` 又可能错过更干净的同主题表达。这里不是简单 theme error，而是同主题 leader 与 peer 选择规则的错误。
3. **risk-control error：** 在 `risk_off` 环境下把单一防御强势股升级为行动建议，可能低估集中度和拥挤交易风险。若没有明确止损/失效条件，正确的防御主题也会变成差的交易。

## 动态选择错误分类

今天存在动态 symbol selection。最可能的未来错误分类如下：

- `0941.HK`：主要风险是 **timing error** 和 **risk-control error**。主题偏防御且相对强，但高位区间导致入场赔率不稳。
- `0857.HK`：若忽视 `symbol_risk_veto` 而升级，属于 **symbol-selection error**，不是 theme confirmation。后验已经明确指出该标的过往选择质量差。
- `0883.HK`：若完全因为 `not_theme_score_leader` 排除而后续跑赢，属于 **symbol-selection error**；需要更细的规则处理“leader 被风险否决时，第二名 peer 是否可作为替代表达”。
- 科技/互联网：若在 MA60 未修复、ETF 未确认时升级，则更可能是 **risk-control error** 和 **theme error**。

## 下一周期优先级调整

1. **先校验数据时间一致性。** 如果 `as_of_date` 与 `quote_trade_date` 不一致，所有行动建议默认降为 `watch_only` 或条件型观察，除非另有真实历史行情校验。
2. **把“风险否决 leader 后的 peer 选择”列为下一轮重点。** 不要因为 leader 被 veto 就自动放弃整个主题，也不要自动买第二名；要求第二名具备独立趋势、成交、相对强度和无后验风险否决。
3. **对高位防御股加入赔率检查。** `range_pos_60 > 0.95` 的 `buy_candidate` 需要更严格的回撤买入触发，避免在拥挤防御交易末端追入。
4. **继续压低低通过率科技/互联网升级倾向。** `0700.HK`、`9988.HK`、`3033.HK`、`3067.HK` 的历史通过率仍不足以支持单名或 ETF 的主动升级，除非 breadth、成交和 MA60 同时修复。

## 记忆更新判断

允许更新 memory。后验里 `symbol_selection_error` 已达 89 次，且今天再次出现了“主题 leader 被风险否决、同主题第二名结构更干净但被机械规则拦截”的具体形态。已有规则覆盖了 peer-relative confirmation，但还不够精确：下一步需要记录“risk-vetoed leader 不应自动代表整个主题；第二名 peer 只能在独立确认后替代”。
