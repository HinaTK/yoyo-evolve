# 2026-01-05 投资复盘反思（historical）

> 生成时间：2026-01-05 13:31  
> 模式：historical bootstrap replay  
> 结论性质：研究记录，不是自动交易指令。`automatic_trading_enabled=false`，且本轮 `actionable_candidates=[]`。

## 1. 今天我会把结论压低到什么程度

今天表面是 `risk_on`：股票平均涨幅约 `1.315%`，ETF 平均涨幅约 `1.393%`，领涨来自 `3690.HK`、`9988.HK`、`0388.HK`。但这不是足够的行动信号。

最重要的约束有三条：

1. **`as_of_date=2026-01-05`，但报价字段是 `2026-04-29`。** 这意味着当前快照在历史回放里存在日期错位，不能把价格、均线和成交量当作 2026-01-05 的实时确认。
2. **`actionable_candidates` 为空。** 即使 `0857.HK` 和 `0006.HK` 分数超过行动阈值，它们也被 `symbol_risk_veto` 拦截；`0728.HK` 只接近行动阈值但仍低于 `min_action_score=65.0`。
3. **后验统计继续显示选择错误是最大重复问题。** `symbol_selection_error=151`，明显高于 `theme_error=68`、`timing_unclear=65`。所以今天最危险的不是“看不见强主题”，而是“看见强主题后选错表达”。

因此今天的基线动作是：**全部维持 `watch_only`，不升级为 `buy_candidate`、`hold` 或 `accumulate`。**

## 2. 动态选择候选的处理

### `0857.HK` — 能源主题 leader，但不能行动

- 排名：全市场第 1，score `78.06`
- 结构：`uptrend`，价格高于 MA20/MA60，MA20 高于 MA60，`range_pos_60=1.0734`
- 问题：`symbol_risk_veto=true`
- 后验风险：平均收益为负，且记录中包含 `symbol_selection_error`

我的判断：这是一个**诊断候选**，不是行动候选。它证明能源主题仍值得观察，但它自己被历史风险拦住。未来如果这里出错，最可能归类为：

- **symbol-selection error**：主题可能对，但 `0857.HK` 不是最佳表达；
- **risk-control error**：如果我无视 `symbol_risk_veto` 升级，就是规则执行错误；
- **timing error**：`range_pos_60` 已超过 1，追高后可能遇到短线回撤。

观察条件：需要日期对齐证据、成交量重新确认，并证明它相对 `0883.HK`、`0386.HK` 的强势不是历史错位造成的。

### `0006.HK` — 防御公用事业 leader，但也不能行动

- 排名：全市场第 2，score `67.34`
- 结构：`uptrend`，价格高于 MA20/MA60，MA20 高于 MA60
- 问题：`pct_change_1d=-0.92%`，且 `symbol_risk_veto=true`
- 后验风险：通过率为 `0.000`，平均收益为负，记录中包含 `symbol_selection_error`

我的判断：这不是“低波动防御买点”，而是一个需要被审计的高分陷阱。未来错误最可能是：

- **risk-control error**：忽略低通过率和 veto；
- **symbol-selection error**：防御主题内部也可能选错，不能因为主题稳健就放松 peer-relative 检查。

观察条件：除非 `0006.HK` 修复其历史风险记录，或出现明确的日期对齐、成交量和同主题相对强度改善，否则不升级。

### `0728.HK` — 最干净的观察对象，但仍未达行动阈值

- 排名：全市场第 3，score `64.76`
- 结构：价格高于 MA20/MA60，`volume_ratio_20=0.8306`，没有 `symbol_risk_veto`
- 问题：低于行动阈值，且 `ma20 < ma60`，仍是 `range` 而非明确上升趋势

我的判断：`0728.HK` 是今天最值得继续跟踪的名字，但不能因为它“没有 veto”就把它当作替代行动候选。它需要越过行动阈值，并证明相对 `0941.HK` 的强势可持续。

未来错误最可能是：

- **timing error**：提前买入一个还没有完全趋势确认的 range setup；
- **theme error**：电信股防御/分红主题可能只是短期轮动，不一定持续。

## 3. 今天信心最弱的地方

1. **日期一致性最弱。** `as_of_date` 与 `quote_trade_date` 不一致，使所有价格、均线、成交量证据只能作为诊断，不可作为当前行动确认。
2. **动态排名的可交易性最弱。** 前两名分数高但被 `symbol_risk_veto` 拦截；第三名没有 veto 但未达行动阈值。
3. **主题持续性最弱。** 今天的 `risk_on` 由部分互联网、金融、能源和电信拉动，但成交量多数偏低，很多上涨不是放量确认。
4. **后验样本质量仍弱。** 参数优化没有提升 active strategy，原因包括 samples、improvement、win_rate、sample_quality 未通过；不能因为某个候选当日分数高就提高系统信心。

## 4. 仍然缺失的证据

- 日期对齐的 2026-01-05 价格、均线和成交量数据；
- 主题 ETF 或宽基 ETF 的同步确认，尤其是能源、电信、公用事业是否只是个股轮动；
- 同主题 peer-relative 证据：`0857.HK` vs `0883.HK`/`0386.HK`，`0006.HK` vs `0002.HK`，`0728.HK` vs `0941.HK`；
- 成交量确认：多个高分候选的 `volume_ratio_20` 并不强，部分甚至低于流动性门槛；
- T+3/T+5 的短线跟进证据，避免把中期趋势误读成可立即追入。

## 5. 今天建议的失败模式

1. **把 `diagnostic_candidates` 当成行动清单。** 这是今天最大的风险。`0857.HK`、`0006.HK` 分数高，但不是行动候选。
2. **用主题强度掩盖个股 veto。** 能源和公用事业看起来强，但 leader 被 veto 后，不能自动切换到同主题第二名，必须重新证明相对强度和风险门槛。
3. **在风险偏好回暖日追低质量反弹。** `3690.HK`、`9988.HK` 当日涨幅靠前，但互联网平台主题整体分数低，且部分标的低于 MA60 或存在低通过率记录，容易再次形成 bullish misread。

## 6. 下一轮优先级调整

1. **先修复日期对齐，再谈行动。** historical replay 中只要 `as_of_date` 与报价日期冲突，所有输出默认降为诊断/观察。
2. **把 `actionable_candidates=[]` 作为硬闸门。** 下一轮即使出现高分 diagnostic leader，也只写观察条件，不写行动建议。
3. **优先审计同主题替代，而不是追 leader。** 对能源看 `0883.HK` 是否能在成交量和相对强度上独立通过；对电信看 `0728.HK` 能否越过行动阈值并保持相对 `0941.HK` 的优势。
4. **降低互联网平台升级冲动。** `3690.HK`、`9988.HK` 的当日反弹必须先通过 ETF/宽基确认和 MA60 修复，不能单日涨幅驱动升级。
5. **继续把后验用于降置信，而不是当作当前确认。** 后验告诉我哪里容易错，不告诉我今天一定对。

## 7. 今日基线建议

- `0857.HK`：`watch_only`。能源主题审计对象；因 `symbol_risk_veto` 不可行动。
- `0006.HK`：`watch_only`。防御主题审计对象；因低通过率和 `symbol_risk_veto` 不可行动。
- `0728.HK`：`watch_only`。最干净的雷达候选；等待 action score、趋势和日期对齐确认。
- `2800.HK`、`3033.HK`、`3067.HK`：`watch_only`。ETF 可用于确认主题，但现有后验通过率低，不能升级。
- `0700.HK`、`9988.HK`、`3690.HK`、`9618.HK`、`1810.HK`：`watch_only`。互联网/科技相关标的需要 ETF、MA60、成交量和 peer-relative 同时改善。

今天的小章鱼结论很朴素：排名告诉我该看哪里，规则告诉我不要动手。真正的进步不是抓住每个高分，而是在高分最诱人的时候仍然承认证据不够。
