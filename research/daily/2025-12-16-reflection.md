# 2025-12-16 投资复盘反思

时间：2025-12-16 20:05  
会话：historical  
策略：`l5_mvp_conservative_v1`  
性质：研究记录，不启用自动交易。

## 1. 今天的市场结构

今天的快照被标记为 `risk_off`。广谱和成长资产偏弱：`2800.HK` 跌 0.99%，`3033.HK` 跌 2.15%，`3067.HK` 跌 2.40%；互联网平台多数仍在下行结构中，`0700.HK`、`9988.HK`、`1024.HK` 都低于 MA20/MA60 或处在弱势区间。相对强势集中在防御、公用事业、电讯和能源：`0006.HK`、`0941.HK`、`0857.HK`、`0883.HK` 的趋势分数较高。

但这份快照有一个非常大的数据弱点：`as_of_date` 是 `2025-12-16`，而多数字段的 `quote_trade_date` 是 `2026-04-28`。所以我不能把价格本身当成严格的 2025-12-16 现场证据；它更像一次历史回放中的结构化基线测试。今天的结论必须偏保守，优先记录规则触发和风险控制，而不是把排名直接翻译成真实交易信号。

## 2. 今日建议

| 标的 | 结论 | 理由 | 失效条件 |
| --- | --- | --- | --- |
| `0006.HK` | 条件型 `buy_candidate` / 小仓观察，执行置信度中低 | 唯一进入 `actionable_candidates`；价格高于 MA20 和 MA60，MA20 高于 MA60，成交量确认，主题为 `utilities-defensive`，没有 `symbol_risk_veto` | 跌回 MA20 下方、`volume_ratio_20` 明显低于 1、或同主题 `0002.HK` 持续跑赢并显示更强相对强度 |
| `0857.HK` | `watch_only`，不升级 | 分数最高但被 `symbol_risk_veto` 拦截；后验显示多次负收益、低通过率和 repeated `symbol_selection_error` | 只有当后续记录显示能源主题仍强、`0857.HK` 相对 `0883.HK` 改善，并且风险 veto 解除，才可重新评估 |
| `0941.HK` | `watch_only`，不升级 | 趋势强，但被 `symbol_risk_veto` 拦截，pass_rate 仍弱 | 需要电讯主题继续确认、成交量维持、且风险记录改善 |
| `2800.HK` / `3033.HK` / `3067.HK` | `watch_only` | 广谱和科技 ETF 均处于弱结构；已有广谱 ETF 与科技 ETF bullish misread 记录，不能在 MA/成交/广度不足时升级 | 重新站上 MA20/MA60，成交放大，且同主题 breadth 改善 |

我今天不会把 `0857.HK` 的最高模型分数当成行动信号。这个点很重要：动态排名把它放在第一，但风险层已经说明它历史选择质量差。今天如果绕过 veto，就会重复过去的 symbol-selection error。

## 3. 信心最弱的位置

1. **数据时间一致性最弱。** `as_of_date` 与 `quote_trade_date` 不一致，削弱了所有价格判断的现场性。这是今天最大的不确定性。
2. **`0006.HK` 的后验样本太薄。** 它是唯一可行动候选，但 posterior summary 里 `0006.HK` 只有 1 个样本，且 pass_rate 为 0.0。样本不足不等于坏标的，但不能给高置信。
3. **防御资产是否已经拥挤。** `0006.HK` 的 `range_pos_60` 为 1.0092，已经接近或超过 60 日区间上沿；趋势强，但短线追高风险也更高。
4. **risk_off 环境下的主题轮动是否延续。** 今天强的是能源、公用事业、电讯，但如果风险偏好快速修复，防御主题可能相对落后。

## 4. 仍然缺失的证据

- 严格对应 2025-12-16 的真实收盘、MA、成交量和指数广度数据。
- `0006.HK` 与 `0002.HK` 在后续 T+3/T+5/T+10 的相对收益证据。
- 公用事业主题是否获得更多成员确认，而不是只有一个模型首选。
- 防御主题相对于 `2800.HK` 的持续超额收益数据。
- 如果考虑能源主题，需要 `0857.HK` 与 `0883.HK` 的 peer-relative 复核，尤其是 `0857.HK` 被 veto 后，不能机械放弃能源，也不能机械切到 `0883.HK`。

## 5. 可能的失败模式

1. **timing error：** `0006.HK` 趋势正确但短线过热，T+3/T+5 先回撤，尤其是 `range_pos_60` 已经高于 1。
2. **symbol-selection error：** 动态选择把 `0006.HK` 作为公用事业 leader，但同主题 `0002.HK` 可能在后续窗口更稳或回撤更小。
3. **risk-control error：** 在 `risk_off` 背景下把唯一 actionable candidate 过度解释成“必须买”，忽略历史回放数据时间不一致和样本过薄。

如果未来这次建议失败，我首先会把它归类为 **timing error 或 symbol-selection error**，而不是直接归咎于公用事业主题错误。主题本身有防御逻辑；更需要检验的是：选择 `0006.HK` 而不是 `0002.HK` 是否有足够 peer-relative 证据，以及是否在高位追入。

## 6. 下一轮优先级调整

1. **先做数据一致性检查。** historical replay 必须把 `as_of_date` 与报价日期不一致作为显式降权项。
2. **对 `0006.HK` 加 peer-relative 复核。** 下一轮比较 `0006.HK` 和 `0002.HK` 的 T+3/T+5 回撤、成交确认和相对强度，避免只相信单一 leader 分数。
3. **继续尊重 `symbol_risk_veto`。** `0857.HK` 和 `0941.HK` 虽然分数高，但 veto 已经说明历史错误重复，不应升级。
4. **不要急着修复科技仓位。** `3033.HK`、`3067.HK`、`0700.HK`、`9988.HK` 仍需要 ETF、MA60 和成交确认；在这些证据出现前，保持 `watch_only`。
5. **把防御主题的“高位拥挤风险”单独记录。** 趋势强不等于安全，尤其当 `range_pos_60` 超过 1 时，应降低短线时机置信度。

## 7. 今日结论

今天最干净的候选是 `0006.HK`，但“最干净”不等于“高置信”。在 `risk_off`、数据时间不一致、样本稀薄、且价格处在高位区间的组合下，我只愿意把它记为条件型小仓候选或重点观察对象。真正的纪律是：让 veto 拦住历史上反复犯错的高分标的，让 peer-relative 复核拦住模型 leader 崇拜，让数据一致性检查拦住历史回放里的假确定性。
