# 2026-01-22 每日推荐报告（historical）

> 模式：candidate recommendation mode（仅候选推荐模式），不是实盘持仓管理；未提供真实持仓与现金比例，因此不把“100% cash”解读为组合决策。  
> 数据口径：本轮为 historical replay。`as_of_date=2026-01-22`，但快照内 `quote_trade_date=2026-04-29`，属于非日期对齐报价；按稳定规则，非日期对齐排名只能作为条件审计项，不可直接升级为交易建议。  
> 策略：`l5_mvp_conservative_v1`；`automatic_trading_enabled=false`，`research_only=true`。  
> 成本门槛：估算往返成本 `35.0 bps`，最低边际收益门槛 `100.0 bps`；没有通过 `actionable_candidates` 的标的不得升级为 `buy_candidate`、`accumulate` 或 `hold`。

## ??????

### ????????
- 今日确定性层结论：`actionable_candidates=[]`，没有任何标的通过可行动门槛。
- 市场状态显示 `risk_state=risk_on`，但稳定规则明确：`risk_on` 不能覆盖 `actionable_candidates=[]`、非日期对齐报价、或 `symbol_risk_veto`。
- 因此本报告没有 `buy_candidate`、`accumulate`、`hold` 级别建议；所有候选最多为 `watch_only` 或 `avoid`。

### ?????????
- 最强雷达主题：`utilities-defensive` 平均分 `75.47`，主题领头为 `0002.HK` / `HKEX:0002`（CLP Holdings / 中电控股，股票），但被 `symbol_risk_veto` 限制为审计观察。
- 次强主题：`telecom-dividend` 平均分 `75.00`，主题领头为 `0941.HK` / `HKEX:0941`（China Mobile / 中国移动，股票），同样被 `symbol_risk_veto` 限制。
- 高分但被否决的动量主题：`energy` 中 `0857.HK` / `HKEX:0857`（PetroChina / 中国石油股份，股票）得分 `84.42`，是全市场最高分，但 `diagnostic_only=true` 且有 `symbol_risk_veto`。

### ??????/??????
- 弱势主题：`consumer-tech`、`healthcare-biotech`、`healthcare-pharma` 平均分均为 `0.00`，主要受 `downtrend_regime`、价格低于均线、低区间位置或风险否决影响。
- 互联网平台虽有个股单日反弹，如 `3690.HK` / `HKEX:3690`（Meituan / 美团-W，股票）涨 `3.55%`、`9988.HK` / `HKEX:9988`（Alibaba / 阿里巴巴-W，股票）涨 `3.24%`，但主题平均分仅 `32.34`，且历史 posterior 显示该主题反复出现 selected-vs-best 错误；不得因单日反弹升级。
- 恒生科技 ETF：`3033.HK` / `HKEX:3033`（CSOP Hang Seng TECH Index ETF / 南方东英恒生科技指数ETF，ETF）与 `3067.HK` / `HKEX:3067`（iShares Hang Seng TECH ETF / 安硕恒生科技ETF，ETF）均低于 watch 分数线，不能作为当前科技风险偏好的确认信号。

## 市场状态

- 市场快照给出 `risk_state=risk_on`。
- 平均涨跌：股票平均 `+1.606%`，ETF 平均 `+1.700%`。
- 领涨个股：`3690.HK` / `HKEX:3690`（Meituan / 美团-W，股票）`+3.55%`；`9988.HK` / `HKEX:9988`（Alibaba / 阿里巴巴-W，股票）`+3.24%`；`0388.HK` / `HKEX:0388`（HKEX / 香港交易所，股票）`+2.99%`。
- 滞后个股：`1093.HK` / `HKEX:1093`（CSPC Pharma / 石药集团，股票）`-0.59%`；`0006.HK` / `HKEX:0006`（Power Assets / 电能实业，股票）`-0.38%`；`1177.HK` / `HKEX:1177`（Sino Biopharm / 中国生物制药，股票）`-0.36%`。

解释：表面风险偏好改善，但报价日期不对齐，且确定性可行动层为空；本轮应视为“雷达审计 + 条件触发清单”，不是开仓日。

## ??????

### 最强雷达主题
1. `utilities-defensive`：平均分 `75.47`，主题领头 `0002.HK` / `HKEX:0002`（CLP Holdings / 中电控股，股票），领头分 `76.08`。价格高于 `ma20` 与 `ma60`，但 posterior 样本显示 pass_rate `0.0`，且 ranking 给出 `symbol_risk_veto`。
2. `telecom-dividend`：平均分 `75.00`，主题领头 `0941.HK` / `HKEX:0941`（China Mobile / 中国移动，股票），领头分 `81.98`。技术形态强，但 pass_rate `0.000 over 30 evaluated calls`，不可行动。
3. `energy`：平均分 `69.67`，主题领头 `0857.HK` / `HKEX:0857`（PetroChina / 中国石油股份，股票），领头分 `84.42`。趋势与量能强，但历史选择错误与不利收益记录触发 veto。

### 最弱雷达主题
- `consumer-tech`：`1810.HK` / `HKEX:1810`（Xiaomi / 小米集团-W，股票）得分 `0`，`downtrend`，价格低于 `ma20` 与 `ma60`。
- `healthcare-biotech`：`2269.HK` / `HKEX:2269`（WuXi Biologics / 药明生物，股票）得分 `0`，`downtrend`，价格低于均线。
- `healthcare-pharma`：`1177.HK` / `HKEX:1177`（Sino Biopharm / 中国生物制药，股票）与 `1093.HK` / `HKEX:1093`（CSPC Pharma / 石药集团，股票）得分 `0`，均处下行结构。

### ?????? 与 ???????????? 的区分
- ??????：排名中的 `qualified_for_watch=true` 只能表示雷达观察，不是交易授权。
- ????????????：`diagnostic_only=true`、`qualified_for_action=false`、`symbol_risk_veto` 或 `actionable_candidates=[]` 的标的只能用于解释市场结构与后续触发条件，不能升级为买入或持有建议。
- 本轮所有高分标的均属于 ????????????，因为 `actionable_candidates=[]` 且前三名均被 `symbol_risk_veto`。

## Top candidates（仅 watch_only / 审计）

### 1. `0857.HK` / `HKEX:0857` — PetroChina / 中国石油股份（股票，theme=`energy`）

- state：`watch_only`
- rationale：确定性排名得分 `84.42`，全市场最高；`trend_score=100`，`momentum_score=75.99`，价格 `11.98` 高于 `ma20=10.853` 与 `ma60=10.2045`，且 `ma20_above_ma60`，趋势结构最强。
- evidence：`pct_change_1d=2.66%`，`volume_ratio_20=1.2356`，`range_pos_60=1.1084`，`regime_flags=["uptrend"]`；主题 `energy` 平均分 `69.67`。
- ???????????????????????：动态选择层把 `0857.HK` 选为 `energy` 主题领头，优于同主题 `0883.HK` / `HKEX:0883`（CNOOC / 中国海洋石油，股票，分数 `73.93`）与 `0386.HK` / `HKEX:0386`（Sinopec / 中国石油化工股份，股票，分数 `50.67`）。不过这只是相对排序，不是行动授权；`0386.HK`虽无 symbol risk veto，但不是主题领头且低于行动分数，不能作为替代交易。
- risks：`symbol_risk_veto`；历史评价 `pass_rate=0.091 over 33 evaluated calls`，`avg_return_pct=-1.658 over 33 evaluated calls`，并有 `record return breached -8.0% adverse threshold` 与 `symbol_selection_error`。
- invalidation：若后续日期对齐快照中跌回 `ma20` 下方、`volume_ratio_20` 低于 `1.0`、或 `range_pos_60` 从高位回落并失去主题领头位置，则取消观察；若仍有 `symbol_risk_veto` 或未进入 `actionable_candidates`，继续不得升级。
- horizon：T+3 至 T+5 观察。
- confidence：低到中。技术分强，但 posterior 风险否决和非日期对齐报价使其只能审计观察。

### 2. `0941.HK` / `HKEX:0941` — China Mobile / 中国移动（股票，theme=`telecom-dividend`）

- state：`watch_only`
- rationale：排名分 `81.98`，`trend_score=100`，价格 `85.45` 高于 `ma20=81.55` 与 `ma60=79.82`，且 `ma20_above_ma60`。防御高息主题在雷达中排名靠前。
- evidence：`pct_change_1d=0.95%`，`volume_ratio_20=1.5041`，`range_pos_60=1.1119`，`regime_flags=["uptrend", "volume-expansion"]`；`telecom-dividend` 主题平均分 `75.00`。
- ???????????????????????：动态选择层选择 `0941.HK` 为 `telecom-dividend` 主题领头，强于同主题 `0728.HK` / `HKEX:0728`（China Telecom / 中国电信，股票，分数 `68.01`）。`0728.HK` 虽有量能扩张，但被标记为 `same_theme_non_leader`、`not_theme_score_leader` 与 `symbol_risk_veto`，不能替代被 veto 的领头。
- risks：`symbol_risk_veto`；历史评价 `pass_rate=0.000 over 30 evaluated calls`，`avg_return_pct=-0.510 over 30 evaluated calls`，并出现不利阈值突破和选择错误记录。高位 `range_pos_60>1` 也增加追高风险。
- invalidation：若后续回落至 `ma20` 下方、量能确认消失、或 `0941.HK` 失去 `telecom-dividend` 主题领头地位，则停止观察；若未进入 `actionable_candidates`，不得升级。
- horizon：T+3 至 T+5 观察。
- confidence：低到中。趋势强，但历史命中率为零且当前仅诊断。

### 3. `0002.HK` / `HKEX:0002` — CLP Holdings / 中电控股（股票，theme=`utilities-defensive`）

- state：`watch_only`
- rationale：排名分 `76.08`，`trend_score=98.22`，价格 `75.85` 高于 `ma20=74.5825` 与 `ma60=73.9937`，防御公用事业主题为全市场平均分最高主题。
- evidence：`pct_change_1d=0.46%`，`volume_ratio_20=1.2769`，`range_pos_60=0.9407`，`regime_flags=["uptrend"]`；`utilities-defensive` 平均分 `75.47`，主题领头 `0002.HK`。
- ???????????????????????：动态选择层选择 `0002.HK` 为 `utilities-defensive` 主题领头，略高于同主题 `0006.HK` / `HKEX:0006`（Power Assets / 电能实业，股票，分数 `74.86`）。`0006.HK` 是同主题非领头，且 `qualified_for_watch=false`、`diagnostic_only=true`、有 `symbol_risk_veto`，不能作为替代行动标的。
- risks：`symbol_risk_veto`；历史评价 `pass_rate=0.000 over 3 evaluated calls`，`avg_return_pct=-0.242 over 3 evaluated calls`。样本较少但方向并未证明有效。
- invalidation：若跌破 `ma20` 或 `ma20` 不再高于 `ma60`，取消观察；若未来仍未解除 `symbol_risk_veto` 或不在 `actionable_candidates`，维持 `watch_only`。
- horizon：T+3 至 T+5 观察。
- confidence：低。防御主题相对强，但历史样本与 veto 不支持行动。

## 其他雷达观察（不得升级）

- `9618.HK` / `HKEX:9618`（JD.com / 京东集团-SW，股票）：互联网平台主题领头，分数 `72.27`，`uptrend`，但 `symbol_risk_veto` 且 `qualified_for_action=false`。仅可用于比较 `0700.HK`、`9988.HK` 等互联网平台标的的相对强弱。
- `0005.HK` / `HKEX:0005`（HSBC / 汇丰控股，股票）：分数 `71.01`，银行主题单一领头，但 `symbol_risk_veto`，历史 `pass_rate=0.000 over 6 evaluated calls`。
- `0388.HK` / `HKEX:0388`（HKEX / 香港交易所，股票）：分数 `67.30`，放量上涨，但 `symbol_risk_veto` 且历史 pass_rate 为 `0.000 over 4 evaluated calls`。
- `2800.HK` / `HKEX:2800`（Tracker Fund of Hong Kong / 盈富基金，ETF）：分数 `56.60`，价格高于 `ma20` 与 `ma60`，但低于行动分数，且 `symbol_risk_veto`；近期 `2026-04-20`、`2026-04-21`、`2026-04-22` 相关窗口出现负收益，不能当作 broad-market ETF fallback。
- `3033.HK` / `HKEX:3033`（CSOP Hang Seng TECH Index ETF / 南方东英恒生科技指数ETF，ETF）与 `3067.HK` / `HKEX:3067`（iShares Hang Seng TECH ETF / 安硕恒生科技ETF，ETF）：恒生科技 ETF 主题平均分 `43.52`，未达 `min_watch_score=45.0`；只能观察，不能确认科技主题。

## Avoids / 回避清单

### 明确回避下行结构
- `0700.HK` / `HKEX:0700`（Tencent / 腾讯控股，股票）：state=`avoid`。`downtrend`，价格 `479.2` 低于 `ma20=498.1` 与 `ma60=526.9`，`range_pos_60=0.0364`，且 `symbol_risk_veto`。失效条件：只有在后续日期对齐数据中重回 `ma20` 与 `ma60` 上方、解除 veto、并进入 `actionable_candidates` 后才可重新评估。
- `1024.HK` / `HKEX:1024`（Kuaishou / 快手-W，股票）：state=`avoid`。`downtrend`，价格低于 `ma20=45.25` 与 `ma60=58.3908`，`range_pos_60=0.0305`，且有历史低 pass_rate。
- `1810.HK` / `HKEX:1810`（Xiaomi / 小米集团-W，股票）：state=`avoid`。`downtrend`，价格 `30.14` 低于 `ma20=31.574` 与 `ma60=33.5827`，`range_pos_60=0.0306`，风险否决存在。
- `6862.HK` / `HKEX:6862`（Haidilao / 海底捞，股票）：state=`avoid`。`downtrend`，价格低于 `ma20=14.715` 与 `ma60=15.9232`，且不是消费可选主题领头。
- `2269.HK` / `HKEX:2269`（WuXi Biologics / 药明生物，股票）：state=`avoid`。`downtrend`，价格低于 `ma20=34.796` 与 `ma60=36.3453`。
- `1177.HK` / `HKEX:1177`（Sino Biopharm / 中国生物制药，股票）：state=`avoid`。`downtrend`，`range_pos_60=-0.0144`，价格低于均线且有 `symbol_risk_veto`。
- `1093.HK` / `HKEX:1093`（CSPC Pharma / 石药集团，股票）：state=`avoid`。`downtrend`，价格低于 `ma20=9.123` 与 `ma60=9.374`，`range_pos_60=0.0791`，且有 `symbol_risk_veto`。

### 回避“替代交易挖掘”
- `0386.HK` / `HKEX:0386`（Sinopec / 中国石油化工股份，股票）虽无 `symbol_risk_veto`，但只是 `energy` 主题第三名，分数 `50.67`，低于行动门槛，且 `not_theme_score_leader`；不能因为 `0857.HK` 和 `0883.HK` 被 veto 就替代升级。
- `3690.HK` / `HKEX:3690`（Meituan / 美团-W，股票）虽单日涨幅最大，但分数 `44.67`，低于 watch 线，并且价格低于 `ma20` 与 `ma60`；不能因领涨而升级。
- `9988.HK` / `HKEX:9988`（Alibaba / 阿里巴巴-W，股票）分数 `44.75`，低于 watch 线，且近期 posterior 有多次 misfire 与 selected-vs-best 错误；不能升级。

## 组合姿态

- 当前为 candidate recommendation mode，不是实盘持仓管理；本报告只输出候选质量、触发条件与风险控制，不做真实仓位调度。
- 建议姿态：不新增可行动交易；维持观察清单，等待日期对齐数据与 `actionable_candidates` 出现。
- 若未来出现候选升级，必须同时满足：标的在配置交易宇宙内；出现在 `actionable_candidates`；`qualified_for_action=true`；不存在 `diagnostic_only=true` 与 `symbol_risk_veto`；预期 swing edge 同时超过 `35.0 bps` 往返成本与 `100.0 bps` 最低边际收益门槛；对同主题历史 best peer 显示新的相对强度，而不是只替代被否决领头。

## 今日结论

本轮虽然表面为 `risk_on`，但确定性交易层没有可行动候选，且高分诊断标的全部被 posterior 风险否决或非日期对齐报价限制。最稳健的结论是：不发出买入、加仓或持有型交易建议；仅保留 `0857.HK`、`0941.HK`、`0002.HK` 作为审计观察，等待日期对齐快照、veto 解除、以及 `actionable_candidates` 重新出现。
