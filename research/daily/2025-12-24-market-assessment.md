# 2025-12-24 港股市场评估

> 会话：historical  
> 角色：yoyo-invest  
> 输出目的：推荐与候选生成，不代表真实持仓管理。  
> 重要数据约束：本次快照标注 `as_of_date=2025-12-24`，但行情字段中的 `quote_trade_date` 多数为 `2026-04-29`。按照稳定规则，日期不一致时，本报告把排名和行情读数视为诊断层证据；即使出现高分候选，也只允许低置信、条件式结论，不能把它当作完整的当日实时确认。

## 1. 市场雷达：先看主题强弱

### 1.1 事实

- 市场雷达总体 `risk_state` 为 `neutral`。
- 雷达样本中：
  - 平均股票 1 日涨跌幅：`+0.633%`
  - 平均 ETF 1 日涨跌幅：`-0.467%`
- 当日雷达领涨：
  1. `1299.HK` 友邦保险：`+4.99%`，`range`，收盘 `87.3`，高于 `ma20=84.9275`，也高于 `ma60=85.1517`。
  2. `2269.HK` 药明生物：`+4.99%`，`range`，收盘 `35.38`，高于 `ma20=34.796`，但低于 `ma60=36.3453`。
  3. `0857.HK` 中国石油股份：`+2.66%`，`uptrend`，收盘 `11.98`，高于 `ma20=10.853` 和 `ma60=10.2045`，`range_pos_60=1.1084`。
- 当日雷达落后：
  1. `2331.HK` 李宁：`-2.79%`。
  2. `3067.HK` 恒生科技 ETF：`-1.97%`，`downtrend`。
  3. `9618.HK` 京东集团：`-1.56%`。
- 主题平均分排名来自 deterministic ranking：
  1. `telecom-dividend`：均分 `60.37`，领导者 `0941.HK`，分数 `69.47`，但被 `symbol_risk_veto` 阻挡。
  2. `financials-bank`：均分 `56.83`，领导者 `0005.HK`，分数 `56.83`。
  3. `utilities-defensive`：均分 `56.48`，领导者 `0006.HK`，分数 `57.34`，但被 `symbol_risk_veto` 阻挡。
  4. `financials-insurance`：均分 `50.71`，领导者 `1299.HK`，分数 `50.71`。
  5. `hong-kong-broad-market`：均分 `46.24`，领导者 `2800.HK`，分数 `46.24`，但被 `symbol_risk_veto` 阻挡。
  6. `energy`：均分 `43.45`，领导者 `0857.HK`，分数 `64.71`，但低于行动阈值且被 `symbol_risk_veto` 阻挡。
  7. `consumer-discretionary`：均分 `17.0`，但内部高度分化，领导者 `2020.HK` 分数 `67.98`，是唯一 deterministic `actionable_candidates`。
  8. `internet-platform`：均分 `9.8`，整体偏弱。
  9. `hang-seng-tech`：均分 `0.0`，ETF 确认偏弱。
  10. `consumer-tech` 与 `healthcare-pharma`：均分 `0.0`。

### 1.2 解读

雷达显示的不是全面风险偏好扩张，而是选择性强势：防御高息、电讯、公用事业、部分金融和能源的趋势读数较好；科技、互联网平台、恒生科技 ETF 和消费科技仍在弱势区。股票平均涨幅为正，但 ETF 平均为负，说明指数层面的确认不足，个股弹性不能直接外推成广泛市场机会。

这对今天的风险姿态很关键：可以研究强势主题，但不应把 `neutral` 市场状态解读成全面加仓信号。尤其在日期字段不一致的历史回放中，所有行动升级都需要额外保守。

## 2. 市场状态与风险姿态

### 2.1 事实

- 市场状态：`neutral`。
- 港股宽基代表 `2800.HK`：
  - 分数 `46.24`，只达到 watch 阈值附近，低于 action 阈值 `65`。
  - 收盘 `25.98`，略高于 `ma20=25.8446`，低于 `ma60=26.1507`。
  - `regime_flags=["range"]`。
  - 有 `symbol_risk_veto`，原因包括 `pass_rate=0.119 over 67 evaluated calls`。
- 恒生科技 ETF：
  - `3033.HK` 分数 `0`，`downtrend`，收盘 `4.718`，低于 `ma20=4.7717` 和 `ma60=4.9835`。
  - `3067.HK` 分数 `0`，`downtrend`，收盘 `9.95`，低于 `ma20=10.2455` 和 `ma60=10.6875`。

### 2.2 解读

宽基没有给出足够强的风险偏好确认，科技 ETF 明确不确认。今天更像“个别低波动/防御/消费单点强势”而不是“港股整体趋势启动”。

因此风险姿态应为：

- 默认 `watch_only`。
- 只允许唯一 deterministic `actionable_candidates` 进入“条件式升级观察”。
- 不追低分主题，不用诊断候选替代行动候选。
- 单一标的上限仍按 balanced profile 控制：即便条件触发，也不超过 `max_single_position_pct=10%`；同主题不超过 `max_theme_exposure_pct=30%`。

## 3. ETF 确认

### 3.1 事实

- `2800.HK` 宽基：`range`，分数 `46.24`，低于行动阈值，并且有历史低通过率风险标记。
- `3033.HK` 与 `3067.HK` 恒生科技 ETF：均为 `downtrend`，分数均为 `0`。
- 科技、互联网平台、消费科技的代表性 ETF/相关标的没有形成上穿 `ma60` 的确认。

### 3.2 解读

ETF 层面不给科技和互联网平台买入确认。稳定规则要求：优先通过 ETF 确认主题，再升级单一个股。今天科技 ETF 反而是反证，所以不能把 `0700.HK`、`9988.HK`、`1810.HK` 的任何单点反弹解读成可行动机会。

宽基 `2800.HK` 只适合作为市场温度计，不适合作为今天的 `buy_candidate`。它略高于 `ma20` 但仍低于 `ma60`，且后验低通过率要求额外确认。

## 4. 雷达主题在交易池内的可行动性

### 4.1 `telecom-dividend`：强，但不可升级

#### 事实

- 交易池内标的：`0941.HK`、`0728.HK`。
- `0941.HK`：
  - 分数 `69.47`，高于 action 阈值。
  - `uptrend`，高于 `ma20=81.55` 和 `ma60=79.82`。
  - `range_pos_60=0.979`，接近 60 日区间高位。
  - 但为 `diagnostic_only=true`，且 `symbol_risk_veto=true`。
  - 风险原因包括：`pass_rate=0.062 over 16 evaluated calls`、平均回报为负、重复 `symbol_selection_error`。
- `0728.HK`：分数 `51.27`，低于 action 阈值；同主题非领导者。

#### 解读

这是雷达中平均分最高的主题，但 deterministic 层不允许升级。`0941.HK` 的趋势强，却被后验风险否决；`0728.HK` 不是更强替代，也没有独立行动分数。最佳表达仍是 `0941.HK`，但只能作为观察对象，不是今日推荐买入。

**状态：`watch_only`。**

### 4.2 `financials-bank`：趋势稳，但成交确认不足

#### 事实

- 交易池内标的：`0005.HK`。
- `0005.HK`：
  - 分数 `56.83`，高于 watch 阈值，低于 action 阈值。
  - `uptrend`，收盘 `142.0`，高于 `ma20=137.425` 和 `ma60=133.1856`。
  - `range_pos_60=0.9203`。
  - disqualifier：`low_volume_ratio_20_below_0_6`。

#### 解读

汇丰是金融银行主题的唯一表达，趋势结构清楚，但分数不足且成交确认弱。它可以继续作为防御金融观察标的，但不能升级为行动候选。

**状态：`watch_only`。**

### 4.3 `utilities-defensive`：主题强，但领导者风险被否决

#### 事实

- 交易池内标的：`0006.HK`、`0002.HK`。
- `0006.HK`：分数 `57.34`，主题领导者，但 `symbol_risk_veto=true`，并且有低成交比率 disqualifier。
- `0002.HK`：分数 `55.62`，同主题非领导者，也有低成交比率和非领导者 disqualifier。

#### 解读

公用事业主题趋势不差，但没有可升级表达。`0006.HK` 被风险历史阻挡；`0002.HK` 不能因为更“干净”就替代领导者，因为它没有打败领导者，也没有满足独立成交与相对强度条件。

**状态：`watch_only`。**

### 4.4 `financials-insurance`：日内强，但不是行动候选

#### 事实

- 交易池内标的：`1299.HK`。
- `1299.HK`：
  - 1 日涨幅 `+4.99%`，雷达领涨之一。
  - 分数 `50.71`，高于 watch 阈值，低于 action 阈值。
  - 高于 `ma20` 与 `ma60`，但 `regime_flags=["range"]`。
  - disqualifier：`low_volume_ratio_20_below_0_6`。

#### 解读

友邦是当天 standout name，但它的强势更像单日跳升后的观察点，而不是已被趋势和量能共同确认的 swing 机会。可列入重点观察，但不升级。

**状态：`watch_only`。**

### 4.5 `energy`：趋势读数强，但行动被否决

#### 事实

- 交易池内标的：`0857.HK`、`0883.HK`、`0386.HK`。
- `0857.HK`：
  - 分数 `64.71`，接近 action 阈值但仍低于 `65`。
  - `uptrend`，1 日涨幅 `+2.66%`。
  - `range_pos_60=1.1084`，已高于近 60 日区间。
  - disqualifiers：低成交比率、`symbol_risk_veto`。
  - 风险原因包括低通过率、平均回报显著为负、出现过不利阈值突破。
- `0883.HK`：分数 `61.86`，趋势良好，但不是主题分数领导者。
- `0386.HK`：分数 `3.78`，不可用。

#### 解读

能源主题有价格趋势，但最佳分数标的 `0857.HK` 被风险历史否决；`0883.HK` 不能自动替代，因为稳定规则要求替代标的必须证明相对领导者的 fresh peer-relative strength。当前证据不足。

**状态：`watch_only`。**

### 4.6 `consumer-discretionary`：唯一可进入升级观察的主题

#### 事实

- 交易池内标的：`2020.HK`、`2331.HK`、`9992.HK`、`6862.HK`。
- `2020.HK` 安踏体育：
  - 是唯一 `actionable_candidates`。
  - 分数 `67.98`，高于 action 阈值 `65`。
  - `trend_score=91.61`，`momentum_score=53.16`，`risk_penalty=0.0`。
  - 收盘 `83.35`，1 日涨幅 `+2.27%`。
  - 高于 `ma20=82.0925` 和 `ma60=80.9417`。
  - `ma20_above_ma60`，`range_pos_60=0.7203`。
  - `regime_flags=["uptrend"]`。
  - 无 disqualifier，`symbol_risk.action_veto=false`。
- 同主题其他标的：
  - `2331.HK`：分数 `0`，`range_pos_60=0.0691`，低于 `ma20` 和 `ma60`。
  - `9992.HK`：分数 `0`，`downtrend`，低于 `ma20` 和 `ma60`。
  - `6862.HK`：分数 `0`，`downtrend`，低于 `ma20` 和 `ma60`。

#### 解读

如果今天必须选一个交易池内最清晰的表达，`2020.HK` 是唯一候选。它不仅是主题领导者，也是唯一通过 deterministic 行动层的标的。与同主题 peers 相比，安踏显著优于李宁、泡泡玛特、海底捞；后者要么趋势弱，要么处于下行。

但日期冲突降低了行动置信度：`2020.HK` 可被列为低置信 `buy_candidate` 的条件观察，而不是无条件买入。

**状态：低置信、条件式 `buy_candidate`。**

### 4.7 `internet-platform` 与 `hang-seng-tech`：不可行动

#### 事实

- `internet-platform` 交易池内标的包括 `0700.HK`、`9988.HK`、`3690.HK`、`1024.HK`、`9618.HK`。
- 主题均分 `9.8`。
- `0700.HK`、`9988.HK`、`1024.HK` 均为 `downtrend` 或明显弱势，并且多项风险/低成交/低通过率标记。
- `9618.HK` 是主题分数领导者，分数 `41.46`，但低于 watch 阈值且有 `symbol_risk_veto`。
- `3033.HK`、`3067.HK` 恒生科技 ETF 均为 `downtrend`，分数为 `0`。

#### 解读

互联网平台和恒生科技今天没有 ETF 确认，也没有可升级个股。稳定规则明确要求：在弱 ETF、低成交、未收复 `ma60` 的选择性风险偏好环境中，科技和互联网平台维持 `watch_only`。

**状态：`watch_only`，不做反弹追逐。**

## 5. 今日候选与推荐状态

### 5.1 可升级层

本次 deterministic `actionable_candidates` 只有一个：`2020.HK`。按照要求，只有这一层有资格进入升级考虑；`0941.HK`、`0857.HK` 等 diagnostic candidates 只能用于解释和观察。

### 5.2 排名表

| 排名 | 标的 | 主题 | 状态 | 依据 | 主要限制 |
|---:|---|---|---|---|---|
| 1 | `2020.HK` 安踏体育 | `consumer-discretionary` | 低置信、条件式 `buy_candidate` | 唯一 `actionable_candidates`；分数 `67.98`；`uptrend`；高于 `ma20`/`ma60`；同主题 peers 明显更弱 | 历史回放日期与 quote 日期不一致；消费可选主题整体均分低，强势集中在单一标的 |
| 2 | `1299.HK` 友邦保险 | `financials-insurance` | `watch_only` | 1 日 `+4.99%`；高于 `ma20`/`ma60` | 分数仅 `50.71`；低成交确认；非行动层 |
| 3 | `0005.HK` 汇丰控股 | `financials-bank` | `watch_only` | 趋势稳定；高于 `ma20`/`ma60` | 分数 `56.83`，低于 action 阈值；低成交确认 |
| 4 | `0941.HK` 中国移动 | `telecom-dividend` | `watch_only` | 主题分数最高；趋势强 | `symbol_risk_veto`；只能诊断观察 |
| 5 | `0857.HK` 中国石油股份 | `energy` | `watch_only` | 能源趋势强；接近 action 阈值 | 低成交比率；`symbol_risk_veto`；历史表现风险重 |

## 6. 具体交易计划：`2020.HK`

### 6.1 事实

- 标的：`2020.HK` 安踏体育。
- 当前读数：收盘 `83.35`。
- 分数：`67.98`。
- 趋势：`uptrend`。
- 均线：
  - `ma20=82.0925`
  - `ma60=80.9417`
  - 价格高于两条均线，且 `ma20_above_ma60`。
- 区间位置：`range_pos_60=0.7203`，处于建设性区间但未极端过热。
- 同主题比较：`2020.HK` 是 `consumer-discretionary` 主题领导者；`2331.HK`、`9992.HK`、`6862.HK` 均未形成可用趋势。

### 6.2 解读

`2020.HK` 是今天唯一结构完整的候选：趋势、分数、相对同主题强度、风险标记都比其他交易池标的更干净。它不是因为主题整体很强而入选，而是因为主题内部出现单一强势表达。

### 6.3 推荐状态

- 推荐状态：低置信、条件式 `buy_candidate`。
- 时间窗口：14–45 天 swing 观察。
- 触发条件：
  1. 价格继续站稳 `ma20=82.0925` 上方；
  2. 后续交易日没有跌回 `ma60=80.9417` 下方；
  3. 同主题 peers 不继续集体破位，至少不能出现消费可选主题全面转弱；
  4. 若能补充日期对齐数据，需要确认 `2025-12-24` 当日或相邻交易日的价格与均线结构一致。
- 初始风险控制：
  - 若进入观察性仓位，单标的不超过组合假设上限 `10%`；在日期不一致未解决前，实际建议应低于上限。
  - 不因单一候选扩大到整个消费可选主题。
- 失效条件：
  - 收盘跌破 `ma60=80.9417`；或
  - 跌破 `ma20=82.0925` 后 1–2 个交易日无法收复；或
  - 同主题相对强度消失，`2020.HK` 不再是主题内最佳表达；或
  - 日期对齐数据证明当前读数并不适用于 `2025-12-24`。
- 信心：低到中低。结构分数支持候选，但日期冲突和主题整体分化要求降级。

## 7. 不推荐升级的重点观察

### 7.1 `0941.HK`

趋势强、分数最高，但 `symbol_risk_veto` 明确阻挡。今天不能因为它分数高就绕过后验风控。观察问题是：它是否能在后续周期改善通过率、减少选择错误，并且保持电讯主题领导地位。

### 7.2 `0857.HK`

能源趋势强，但过往样本表现很差，且本次低成交比率与 `symbol_risk_veto` 同时存在。不能把接近 action 阈值当作行动信号。`0883.HK` 虽较干净，但不是自动替代。

### 7.3 `1299.HK`

单日领涨值得研究，但不是 deterministic 行动候选。需要确认这不是一次 headline-driven move 或短期跳升后的回落。

### 7.4 `2800.HK`、`3033.HK`、`3067.HK`

宽基和科技 ETF 不给出行动确认。`2800.HK` 只是 range；`3033.HK` 和 `3067.HK` 都在 downtrend。今天不能用 ETF 作为加风险依据。

## 8. 外部机会与交易池覆盖

本次雷达强主题基本都已在交易池内有代表标的：电讯、金融、公用事业、能源、消费可选、医疗、生物科技、宽基与科技 ETF 均有覆盖。因此没有明显“雷达强但交易池完全缺席”的主题。

如果后续希望扩展交易池，可考虑增加更多同主题替代表达，尤其是：

- `telecom-dividend`：用于在 `0941.HK` 被风险否决时比较是否存在更干净的高息电讯表达。
- `energy`：用于在 `0857.HK` 被风险否决时测试是否存在比 `0883.HK` 更强、更稳定的替代。
- `consumer-discretionary`：用于验证 `2020.HK` 是否只是单一标的强势，还是运动服饰/消费复苏子主题扩散。

这些是以后可加入研究池的外部机会，不是今天的即时推荐。

## 9. 今日风险结论

### 事实

- 市场状态 `neutral`。
- ETF 层面没有全面风险偏好确认。
- 唯一 deterministic `actionable_candidates` 是 `2020.HK`。
- 多个高分诊断候选被 `symbol_risk_veto` 或低成交确认阻挡。
- `as_of_date` 与 `quote_trade_date` 不一致。

### 解读

今天不适合大范围升级。最佳动作是保持防守式研究节奏：只把 `2020.HK` 放入低置信条件式 `buy_candidate`，其余强主题只做观察。科技和互联网平台继续 `watch_only`，不追反弹。

## 10. 今日高优先级研究问题

1. `2020.HK` 在日期对齐的 `2025-12-24` 数据中，是否同样高于 `ma20` 与 `ma60`，并保持同主题相对强度第一？
2. `consumer-discretionary` 的强势是否只集中在 `2020.HK`，还是有运动服饰子主题扩散迹象？重点比较 `2020.HK` 与 `2331.HK` 的相对强弱。
3. `0941.HK` 被 `symbol_risk_veto` 后，`telecom-dividend` 是否存在更干净的同主题替代，还是整个主题只能观察？
4. `1299.HK` 的 `+4.99%` 是否有后续成交和价格跟进，还是一次不可追的单日跳升？
5. `2800.HK` 能否重新站上 `ma60=26.1507`，从而给宽基风险偏好提供确认？
